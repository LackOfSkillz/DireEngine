
Aedan — AreaForge System Brief (Tasks 921–1000)

We are building a subsystem called AreaForge inside DireMud.

The goal is to allow you (Aedan, running via Copilot in VS Code) to take a map image—such as a DragonRealms-style city map—and convert it into a fully playable in-game area automatically.

This is NOT just a parser.

It is a multi-stage, AI-assisted world construction pipeline.

🎯 Core Objective

Given:

A map image (e.g., maps/xyz.jpg)

You should be able to:

Extract spatial structure (rooms + exits)
Interpret map semantics (labels, landmarks, transitions)
Generate meaningful names and atmosphere
Build a complete Evennia area
Produce reviewable artifacts for iteration

And do this with a single command.

🧠 System Philosophy

AreaForge is built on a hybrid model:

Deterministic Extraction (code-driven)
Node detection (rooms)
Edge detection (connections)
Coordinate mapping
Graph construction
Semantic Interpretation (OCR + heuristics)
Read map text (labels, gates, bridges, etc.)
Associate text with rooms and exits
Detect special transitions
AI Adjudication (your role)
Refine weak or ambiguous labels
Improve naming and consistency
Generate atmospheric descriptions
Resolve uncertainty safely (do not guess blindly)
Validation + Build
Produce structured AreaSpec (JSON)
Generate review report (flags, warnings)
Build/update Evennia objects
Snapshot and diff changes
🧱 What Was Built (Tasks 921–1000)

You now have:

OCR Subsystem (921–940)
Extract text from maps using Tesseract
Group text into readable labels
Detect:
place names
special exits (e.g., "go gate", "climb stair")
Attach text to nodes and edges with confidence scoring
Graph + Semantic Layer (941–960)
Nodes and edges enriched with OCR data
Confidence scoring system (high / medium / low)
Special exits preserved from OCR
AreaSpec generated as structured output
Review report flags weak/uncertain data
Adjudication Layer (961–980)
You refine:
weak labels
ambiguous exits
missing descriptions
High-confidence OCR is preserved
Fallbacks ensure completeness (no broken areas)
Atmosphere + prose baseline applied
Automation + CLI System (981–1000)
Manifest-driven builds
One-command execution pipeline
Modes:
extract
enrich
build
full
Snapshot + diff system for safe iteration
Standardized output artifacts
⚙️ Your Operational Role

You are not just executing code.

You are acting as:

Area Synthesis Operator + Adjudication Engine

When running AreaForge, you must:

Run the pipeline (usually --mode full)
Inspect outputs:
AreaSpec
Review report
Diff summary
Make decisions:
Are labels accurate?
Are exits logical?
Are any clusters disconnected?
Improve where needed:
Refine labels (especially medium/low confidence)
Adjust naming consistency
Improve room descriptions
Re-run safely (system is idempotent)
⚠️ Critical Rules
Do NOT blindly trust OCR
Do NOT overwrite high-confidence data unnecessarily
Do NOT invent structure that conflicts with geometry
Always preserve stable IDs (based on coordinates)
Prefer consistency over creativity when uncertain
Always produce reviewable output before final build
📦 Expected Outputs Per Run

After execution, you should produce:

Manifest file
manifests/<area_id>.yaml
AreaSpec
build/areaspec.json
Review report
build/review.txt
Snapshot
build/snapshots/<area_id>.json
Console summary:
room count
exit count
review flags
diff results
🚀 Target Workflow

User says:

"Aedan, I added maps/xyz.jpg. Build area the_landing."

You should:

Generate or load manifest
Run AreaForge (--mode full)
Review results (especially flags)
Apply adjudication improvements if needed
Build/update Evennia area
Report:
what you did
what was generated
what needs attention
🧠 End State

When working correctly, AreaForge turns:

→ a static map image

into:

→ a structured, named, atmospheric, playable in-game area
→ with reproducible builds and safe iteration

This system is foundational.

It will eventually support:

cities
towns
castles
dungeons
world-scale imports

Your job is to operate it precisely and improve output quality over time.

Proceed methodically.



Microtasks 921–940 — AreaForge OCR Subsystem (Atomic, Aedan-Ready)

🧱 SECTION 1 — OCR Foundation + Dependency Wiring (921–925)

🧱 MT921 — Create OCR module scaffold
File:
world/area_forge/extract/ocr.py

Add:

from pathlib import Path

def extract_ocr_regions(image_path):
    """
    Return OCR text regions for a map image.

    Output:
    [
        {
            "text": "Town Green",
            "x": 120,
            "y": 240,
            "w": 80,
            "h": 18,
            "confidence": 0.91
        }
    ]
    """
    return []

Validation:

Module imports cleanly
Function exists
Returns list

🧱 MT922 — Add OCR dependency guards
File:
world/area_forge/extract/ocr.py

Add:

try:
    import pytesseract
except ImportError:  # pragma: no cover
    pytesseract = None

try:
    from PIL import Image
except ImportError:  # pragma: no cover
    Image = None

Validation:

Missing OCR libs do not crash import
Runtime can detect unavailable OCR stack

🧱 MT923 — Add OCR availability helper
File:
world/area_forge/extract/ocr.py

Add:

def ocr_available():
    return pytesseract is not None and Image is not None

Validation:

Returns boolean
Safe to call from parser pipeline

🧱 MT924 — Add explicit OCR runtime failure
Modify extract_ocr_regions():

def extract_ocr_regions(image_path):
    if not ocr_available():
        raise RuntimeError("OCR dependencies are not installed. Install pytesseract and Pillow.")
    return []

Validation:

Failure is explicit
No silent OCR fallback

🧱 MT925 — Add OCR install note to AreaForge docs
File:
world/area_forge/README.md or equivalent subsystem docs

Document:

pip install pytesseract pillow
system-level Tesseract install requirement
how Aedan should verify OCR is available

Validation:

OCR setup is documented
Aedan has a clear setup path

🧱 SECTION 2 — Raw OCR Extraction (926–930)

🧱 MT926 — Implement basic OCR text-region extraction
File:
world/area_forge/extract/ocr.py

Replace scaffold with:

def extract_ocr_regions(image_path):
    if not ocr_available():
        raise RuntimeError("OCR dependencies are not installed. Install pytesseract and Pillow.")

    image = Image.open(image_path)
    data = pytesseract.image_to_data(image, output_type=pytesseract.Output.DICT)

    results = []
    for i in range(len(data["text"])):
        text = data["text"][i].strip()
        if not text:
            continue

        conf = float(data["conf"][i]) if data["conf"][i] not in ("", "-1") else -1
        if conf < 0:
            continue

        results.append({
            "text": text,
            "x": int(data["left"][i]),
            "y": int(data["top"][i]),
            "w": int(data["width"][i]),
            "h": int(data["height"][i]),
            "confidence": conf / 100.0,
        })

    return results

Validation:

OCR returns text region dicts
Empty strings are skipped
Confidence is normalized

🧱 MT927 — Add minimum confidence filter
File:
world/area_forge/extract/ocr.py

Update extraction:

MIN_OCR_CONFIDENCE = 0.45
...
if (conf / 100.0) < MIN_OCR_CONFIDENCE:
    continue

Validation:

Low-confidence garbage is reduced
OCR output is cleaner

🧱 MT928 — Normalize OCR text whitespace
File:
world/area_forge/extract/ocr.py

Add helper:

def normalize_ocr_text(text):
    return " ".join(text.split()).strip()

Use it during extraction:

text = normalize_ocr_text(data["text"][i])

Validation:

Double spaces removed
OCR text is normalized consistently

🧱 MT929 — Add image path validation
File:
world/area_forge/extract/ocr.py

Add at top of extractor:

image_path = Path(image_path)
if not image_path.exists():
    raise FileNotFoundError(f"OCR image not found: {image_path}")

Validation:

Bad input path fails clearly
No confusing PIL traceback for missing files

🧱 MT930 — Add OCR debug output helper
File:
world/area_forge/extract/ocr.py

Add:

def dump_ocr_regions(regions):
    for region in regions:
        print(
            f"{region['text']!r} @ "
            f"({region['x']},{region['y']}) "
            f"{region['w']}x{region['h']} "
            f"conf={region['confidence']:.2f}"
        )

Validation:

Aedan can inspect OCR output quickly
Useful for parser tuning

🧱 SECTION 3 — OCR Grouping + Semantic Cleanup (931–935)

🧱 MT931 — Group OCR words into lines
File:
world/area_forge/extract/ocr.py

Add:

def group_ocr_lines(regions, y_tolerance=12):
    lines = []

    for region in sorted(regions, key=lambda item: (item["y"], item["x"])):
        matched = None
        for line in lines:
            if abs(region["y"] - line["y"]) <= y_tolerance:
                matched = line
                break

        if matched is None:
            matched = {
                "text_parts": [],
                "x": region["x"],
                "y": region["y"],
                "w": region["w"],
                "h": region["h"],
                "confidence_values": [],
            }
            lines.append(matched)

        matched["text_parts"].append((region["x"], region["text"]))
        matched["x"] = min(matched["x"], region["x"])
        matched["y"] = min(matched["y"], region["y"])
        matched["w"] = max(matched["w"], region["x"] + region["w"] - matched["x"])
        matched["h"] = max(matched["h"], region["h"])
        matched["confidence_values"].append(region["confidence"])

    output = []
    for line in lines:
        ordered = [part for _x, part in sorted(line["text_parts"], key=lambda item: item[0])]
        output.append({
            "text": normalize_ocr_text(" ".join(ordered)),
            "x": line["x"],
            "y": line["y"],
            "w": line["w"],
            "h": line["h"],
            "confidence": sum(line["confidence_values"]) / max(len(line["confidence_values"]), 1),
        })

    return output

Validation:

Single-word OCR becomes line-level labels
Nearby horizontal text gets merged

🧱 MT932 — Add OCR noise filter for trivial tokens
File:
world/area_forge/extract/ocr.py

Add:

def is_noise_text(text):
    cleaned = text.strip()
    if len(cleaned) <= 1:
        return True
    if cleaned in {"|", "-", "_", ".", ",", ":"}:
        return True
    return False

Use after line grouping:

lines = [line for line in lines if not is_noise_text(line["text"])]

Validation:

Tiny junk OCR artifacts are filtered out
Map title/label pass is cleaner

🧱 MT933 — Add special-exit keyword detector
File:
world/area_forge/extract/ocr.py

Add:

SPECIAL_EXIT_KEYWORDS = {
    "gate",
    "arch",
    "bridge",
    "stair",
    "stairs",
    "dock",
    "pier",
    "path",
    "ferry",
    "ramp",
}

And helper:

def detect_special_exit_labels(lines):
    matches = []
    for line in lines:
        lower = line["text"].lower()
        if any(word in lower for word in SPECIAL_EXIT_KEYWORDS):
            matches.append(line)
    return matches

Validation:

OCR can surface likely exit-command labels
Special transition hints are detectable

🧱 MT934 — Add place-name candidate detector
File:
world/area_forge/extract/ocr.py

Add:

def detect_place_name_candidates(lines):
    candidates = []
    for line in lines:
        text = line["text"]
        words = text.split()
        if len(words) >= 2 and any(word[:1].isupper() for word in words if word):
            candidates.append(line)
    return candidates

Validation:

Multi-word location labels can be separated from raw OCR
Better inputs for room naming

🧱 MT935 — Return structured OCR bundle
File:
world/area_forge/extract/ocr.py

Add:

def extract_ocr_bundle(image_path):
    raw_regions = extract_ocr_regions(image_path)
    lines = group_ocr_lines(raw_regions)

    return {
        "raw_regions": raw_regions,
        "lines": lines,
        "special_exit_labels": detect_special_exit_labels(lines),
        "place_name_candidates": detect_place_name_candidates(lines),
    }

Validation:

OCR subsystem returns one structured payload
Parser can consume OCR in stages

🧱 SECTION 4 — Parser Integration Hooks (936–940)

🧱 MT936 — Add OCR toggle to AreaForge build pipeline
File:
Current map parser / builder entrypoint

Add a setting or flag:

USE_OCR = True

And in build flow:

ocr_bundle = None
if USE_OCR:
    ocr_bundle = extract_ocr_bundle(MAP_PATH)

Validation:

OCR can be enabled/disabled cleanly
Existing parser still runs without OCR

🧱 MT937 — Attach nearest OCR label to detected nodes
File:
Current map parser module

Add helper:

def attach_ocr_labels_to_nodes(nodes, ocr_lines, max_distance=80):
    for node in nodes:
        best_line = None
        best_distance = None

        for line in ocr_lines:
            dx = node["x"] - line["x"]
            dy = node["y"] - line["y"]
            dist = (dx * dx + dy * dy) ** 0.5

            if dist <= max_distance and (best_distance is None or dist < best_distance):
                best_line = line
                best_distance = dist

        node["ocr_label"] = best_line["text"] if best_line else None
        node["ocr_label_confidence"] = best_line["confidence"] if best_line else 0.0

Validation:

Nodes gain nearby OCR labels
Parser can use semantic hints without changing node detection

🧱 MT938 — Prefer OCR label in room naming when confidence is good
File:
Current room naming helper, likely _room_key()

Modify:

def _room_key(node, road_meta):
    if node.get("ocr_label") and node.get("ocr_label_confidence", 0) >= 0.70:
        return node["ocr_label"]
    ...

Validation:

Strong OCR labels override generic generated names
Existing procedural naming still works as fallback

🧱 MT939 — Add OCR debug summary to build output
File:
Current build entrypoint / AreaForge runner

Add output like:

if ocr_bundle:
    print(f"OCR lines found: {len(ocr_bundle['lines'])}")
    print(f"Special exit labels found: {len(ocr_bundle['special_exit_labels'])}")
    print(f"Place-name candidates found: {len(ocr_bundle['place_name_candidates'])}")

Validation:

Aedan can see whether OCR found useful signal
Easier tuning per map

🧱 MT940 — Full OCR subsystem validation
Confirm:

OCR module imports cleanly
OCR extraction works on map image
Lines are grouped from raw OCR words
Noise labels are filtered
Special-exit labels are detected
Place-name candidates are detected
Nodes receive nearest OCR labels
Room naming can use OCR when confidence is high
Parser still works when OCR is disabled or unavailable

🎯 Result After 921–940

You now have:

✔ A real OCR subsystem inside AreaForge
✔ Structured OCR extraction instead of ad hoc parsing
✔ Line grouping and label cleanup
✔ Special-exit and place-name hint detection
✔ Parser hooks to attach OCR semantics to map nodes
✔ Safe fallback when OCR is unavailable

🧠 Big System Shift

You now moved from:

geometry-only map parsing

to:

geometry + semantic text extraction

🚀 Next Best Step

👉 941–960 — OCR-to-graph association + review-report generation

That should cover:

binding OCR labels to roads/districts/exits more accurately
confidence scoring
review artifacts for Aedan before import
send 941-960
Microtasks 941–960 — OCR → Graph Association + Review System (Atomic, Aedan-Ready)

🧱 SECTION 1 — OCR Confidence + Scoring Layer (941–945)

🧱 MT941 — Add OCR confidence scoring helper
File:
world/area_forge/model/confidence.py (NEW)

Add:

def score_label_confidence(distance, ocr_conf):
    """
    Combine spatial distance + OCR confidence into a single score.
    """
    if distance is None:
        return 0.0

    distance_score = max(0.0, 1.0 - (distance / 100.0))
    return (distance_score * 0.6) + (ocr_conf * 0.4)

Validation:

Returns 0–1 score
Penalizes far labels
Boosts strong OCR confidence

🧱 MT942 — Store association confidence on nodes
File:
Parser module where nodes are enriched

Modify:

node["ocr_distance"] = best_distance if best_line else None
node["ocr_association_score"] = score_label_confidence(
    best_distance,
    best_line["confidence"] if best_line else 0
)

Validation:

Each node has:
distance
association score
Values are stable and numeric

🧱 MT943 — Add confidence thresholds
File:
world/area_forge/model/confidence.py

Add:

OCR_HIGH_CONF = 0.75
OCR_MED_CONF = 0.5
OCR_LOW_CONF = 0.3

Validation:

Centralized thresholds
Used consistently across system

🧱 MT944 — Classify OCR reliability tier
Add:

def classify_confidence(score):
    if score >= OCR_HIGH_CONF:
        return "high"
    elif score >= OCR_MED_CONF:
        return "medium"
    elif score >= OCR_LOW_CONF:
        return "low"
    return "none"

Validation:

Returns expected string tiers
Used in review reporting

🧱 MT945 — Attach confidence tier to node
Modify node enrichment:

node["ocr_confidence_tier"] = classify_confidence(node.get("ocr_association_score", 0))

Validation:

Nodes now categorized:
high / medium / low / none

🧱 SECTION 2 — OCR → Edge / Exit Association (946–950)

🧱 MT946 — Attach OCR labels to edges
File:
Parser module

Add:

def attach_ocr_labels_to_edges(edges, nodes, ocr_lines):
    node_lookup = {n["id"]: n for n in nodes}

    for edge in edges:
        source = node_lookup[edge[0]]
        target = node_lookup[edge[2]]

        mid_x = (source["x"] + target["x"]) / 2
        mid_y = (source["y"] + target["y"]) / 2

        best = None
        best_dist = None

        for line in ocr_lines:
            dx = mid_x - line["x"]
            dy = mid_y - line["y"]
            dist = (dx * dx + dy * dy) ** 0.5

            if best_dist is None or dist < best_dist:
                best = line
                best_dist = dist

        edge_label = best["text"] if best else None
        edge_conf = best["confidence"] if best else 0

        edge_data = {
            "label": edge_label,
            "confidence": edge_conf,
            "distance": best_dist
        }

        yield (*edge, edge_data)

Validation:

Edges gain OCR label candidates
Midpoint-based association works

🧱 MT947 — Detect explicit exit commands from OCR
File:
ocr.py

Add:

def is_exit_command(text):
    text = text.lower()
    return text.startswith("go ") or text.startswith("climb ")

Validation:

Identifies DR-style commands like:
go gate
climb stair

🧱 MT948 — Promote OCR exit labels to edge metadata
Modify edge enrichment:

if edge_data["label"] and is_exit_command(edge_data["label"]):
    edge_data["is_command"] = True

Validation:

Special exits are flagged
Parser can later convert to Evennia exit keys

🧱 MT949 — Override procedural exit names with OCR
File:
exit naming logic

Modify:

if edge_data.get("is_command"):
    exit_name = edge_data["label"]
else:
    exit_name = generated_name

Validation:

OCR “go gate” overrides guessed names
Procedural fallback remains

🧱 MT950 — Store edge confidence tier
Add:

edge_data["confidence_tier"] = classify_confidence(
    score_label_confidence(edge_data["distance"], edge_data["confidence"])
)

Validation:

Edge labeling has same confidence system as nodes

🧱 SECTION 3 — AreaSpec + Review Artifacts (951–955)

🧱 MT951 — Create AreaSpec serializer
File:
world/area_forge/serializer.py (NEW)

Add:

import json

def save_area_spec(path, data):
    with open(path, "w") as f:
        json.dump(data, f, indent=2)

Validation:

JSON file is created
Data is readable

🧱 MT952 — Build AreaSpec structure
File:
builder pipeline

Add:

area_spec = {
    "nodes": nodes,
    "edges": edges,
    "meta": {
        "ocr_used": True,
        "node_count": len(nodes),
        "edge_count": len(edges)
    }
}

Validation:

AreaSpec exists
Contains core data

🧱 MT953 — Add review flags generator
File:
world/area_forge/review.py (NEW)

Add:

def generate_review_flags(nodes, edges):
    flags = []

    for node in nodes:
        if node.get("ocr_confidence_tier") == "low":
            flags.append(f"Low-confidence label: {node.get('ocr_label')}")

    for edge in edges:
        if isinstance(edge, tuple) and len(edge) > 3:
            data = edge[3]
            if data.get("confidence_tier") == "low":
                flags.append(f"Uncertain exit label: {data.get('label')}")

    return flags

Validation:

Flags produced for weak OCR associations

🧱 MT954 — Generate review report file
File:
review.py

Add:

def save_review_report(path, flags):
    with open(path, "w") as f:
        for flag in flags:
            f.write(f"- {flag}\n")

Validation:

Human-readable report generated
Flags listed line-by-line

🧱 MT955 — Wire review system into pipeline
Modify build flow:

flags = generate_review_flags(nodes, edges)

save_area_spec("build/areaspec.json", area_spec)
save_review_report("build/review.txt", flags)

Validation:

AreaSpec + review report created every run
Aedan can inspect output

🧱 SECTION 4 — CLI + Aedan Workflow Integration (956–960)

🧱 MT956 — Add CLI entrypoint for AreaForge
File:
world/area_forge/cli/forge_area.py

Add:

import argparse

def main():
    parser = argparse.ArgumentParser()
    parser.add_argument("--map", required=True)
    parser.add_argument("--area-id", required=True)
    args = parser.parse_args()

    run_area_forge(args.map, args.area_id)

Validation:

CLI callable via python -m ...
Accepts map + area id

🧱 MT957 — Add OCR toggle flag
Modify CLI:

parser.add_argument("--no-ocr", action="store_true")

Pass to pipeline:

use_ocr = not args.no_ocr

Validation:

OCR can be disabled for testing
Pipeline still works

🧱 MT958 — Add summary output
After build:

print(f"Rooms: {len(nodes)}")
print(f"Exits: {len(edges)}")
print(f"Review flags: {len(flags)}")

Validation:

Aedan gets quick feedback
No need to open files for summary

🧱 MT959 — Add artifact paths output
Add:

print("Artifacts:")
print(" - build/areaspec.json")
print(" - build/review.txt")

Validation:

Aedan knows where outputs are
Consistent artifact structure

🧱 MT960 — Full OCR + Graph + Review validation

Confirm:

✔ OCR extracts text regions
✔ OCR lines grouped correctly
✔ Nodes receive OCR labels + confidence
✔ Edges receive OCR labels (midpoint detection)
✔ Special exits detected from OCR commands
✔ Confidence scoring works for nodes + edges
✔ AreaSpec JSON is generated
✔ Review report flags low-confidence issues
✔ CLI runs full pipeline
✔ OCR can be toggled off
✔ System produces stable, reviewable outputs

🎯 Result After 941–960

You now have:

✔ OCR fully integrated into AreaForge
✔ Semantic labeling for both rooms and exits
✔ Confidence scoring system
✔ Review pipeline for Aedan
✔ Structured AreaSpec output
✔ CLI workflow ready for VS Code automation

🧠 Big System Shift

You now moved from:

“map parsing”

to:

map extraction + semantic interpretation + reviewable synthesis

🚀 Next Step

👉 961–980 — Aedan Adjudication Layer

This will let Copilot:

rewrite weak labels
improve descriptions
resolve ambiguous exits
finalize AreaSpec before import

That’s where the system becomes truly intelligent.

send 961-980
Microtasks 961–980 — Aedan Adjudication Layer (AI-in-the-loop Refinement)

🧱 SECTION 1 — Adjudication Framework (961–965)

🧱 MT961 — Create adjudication module scaffold
File:
world/area_forge/ai/adjudicator.py

Add:

def adjudicate_area_spec(area_spec, context=None):
    """
    Refine AreaSpec using AI or rule-based adjudication.
    Returns updated AreaSpec.
    """
    return area_spec

Validation:

Module imports cleanly
Function callable from pipeline

🧱 MT962 — Add adjudication toggle to pipeline
File:
AreaForge build pipeline

Add:

USE_AI_ADJUDICATION = True

And:

if USE_AI_ADJUDICATION:
    area_spec = adjudicate_area_spec(area_spec)

Validation:

Adjudication layer optional
Pipeline still runs without it

🧱 MT963 — Pass context into adjudicator
Modify call:

area_spec = adjudicate_area_spec(area_spec, context={
    "profile": profile,
    "area_id": area_id,
    "style": style_settings,
})

Validation:

Adjudicator receives metadata
Enables smarter decisions

🧱 MT964 — Add adjudication logging hook
File:
adjudicator.py

Add:

def log_adjudication(message):
    print(f"[AreaForge AI] {message}")

Validation:

Logs appear during pipeline
Aedan can trace decisions

🧱 MT965 — Add no-op fallback adjudicator
Modify:

def adjudicate_area_spec(area_spec, context=None):
    log_adjudication("Adjudication pass (no-op fallback)")
    return area_spec

Validation:

Safe baseline behavior
No unintended mutations yet

🧱 SECTION 2 — Node Label Refinement (966–970)

🧱 MT966 — Add low-confidence label rewrite hook
File:
adjudicator.py

Add:

def refine_node_labels(nodes):
    for node in nodes:
        if node.get("ocr_confidence_tier") in ("low", "none"):
            node["needs_label_review"] = True
    return nodes

Validation:

Weak labels flagged
No overwrite yet

🧱 MT967 — Improve medium-confidence labels
Add:

def enhance_medium_labels(nodes):
    for node in nodes:
        if node.get("ocr_confidence_tier") == "medium":
            node["label_candidate"] = node.get("ocr_label")
    return nodes

Validation:

Medium labels preserved for later improvement
No destructive overwrite

🧱 MT968 — Preserve high-confidence OCR labels
Add:

def lock_high_confidence_labels(nodes):
    for node in nodes:
        if node.get("ocr_confidence_tier") == "high":
            node["final_label"] = node.get("ocr_label")
    return nodes

Validation:

Strong OCR labels protected
Prevents AI from overriding good data

🧱 MT969 — Add procedural fallback labeling
Add:

def fallback_labels(nodes):
    for node in nodes:
        if not node.get("final_label"):
            node["final_label"] = node.get("generated_name", node["id"])
    return nodes

Validation:

Every node gets a final label
System remains complete even without OCR

🧱 MT970 — Combine label refinement pipeline
Modify adjudicator:

nodes = area_spec["nodes"]
nodes = lock_high_confidence_labels(nodes)
nodes = enhance_medium_labels(nodes)
nodes = refine_node_labels(nodes)
nodes = fallback_labels(nodes)

area_spec["nodes"] = nodes

Validation:

Labels flow through deterministic refinement stages
No missing labels

🧱 SECTION 3 — Exit / Edge Adjudication (971–975)

🧱 MT971 — Flag low-confidence exits
File:
adjudicator.py

Add:

def flag_uncertain_exits(edges):
    for edge in edges:
        data = edge[3] if len(edge) > 3 else {}
        if data.get("confidence_tier") in ("low", "none"):
            data["needs_review"] = True
    return edges

Validation:

Weak exits clearly marked
Review system strengthened

🧱 MT972 — Preserve OCR command exits
Add:

def preserve_command_exits(edges):
    for edge in edges:
        data = edge[3] if len(edge) > 3 else {}
        if data.get("is_command"):
            data["final_exit_name"] = data["label"]
    return edges

Validation:

“go gate” / “climb stair” preserved
No procedural override

🧱 MT973 — Fallback directional exits
Add:

def fallback_exit_names(edges):
    for edge in edges:
        data = edge[3] if len(edge) > 3 else {}
        if not data.get("final_exit_name"):
            data["final_exit_name"] = edge[1]  # direction
    return edges

Validation:

Every exit has a usable name
Compatible with Evennia

🧱 MT974 — Add exit consistency check
Add:

def check_exit_symmetry(edges):
    seen = set()
    for edge in edges:
        key = (edge[0], edge[2])
        reverse = (edge[2], edge[0])
        if reverse not in seen:
            edge_data = edge[3] if len(edge) > 3 else {}
            edge_data["one_way_warning"] = True
        seen.add(key)
    return edges

Validation:

Detects one-way exits
Flags asymmetry issues

🧱 MT975 — Combine edge adjudication pipeline
Modify adjudicator:

edges = area_spec["edges"]
edges = preserve_command_exits(edges)
edges = fallback_exit_names(edges)
edges = flag_uncertain_exits(edges)
edges = check_exit_symmetry(edges)

area_spec["edges"] = edges

Validation:

Edges fully normalized
Review signals attached

🧱 SECTION 4 — Prose + Atmosphere Adjudication (976–980)

🧱 MT976 — Add prose seed normalization
File:
adjudicator.py

Add:

def normalize_prose_seed(nodes):
    for node in nodes:
        node["prose_seed"] = node.get("prose_seed", {})
    return nodes

Validation:

Every node has prose_seed dict
Safe for later expansion

🧱 MT977 — Add atmosphere tagging
Add:

def assign_atmosphere_tags(nodes):
    for node in nodes:
        seed = node.get("prose_seed", {})
        if "market" in seed.get("district", ""):
            node["atmosphere"] = "busy_commercial"
        else:
            node["atmosphere"] = "neutral"
    return nodes

Validation:

Basic thematic tagging exists
Extensible later

🧱 MT978 — Add placeholder prose refinement hook
Add:

def refine_prose(nodes):
    for node in nodes:
        if not node.get("desc_final"):
            node["desc_final"] = f"You are at {node.get('final_label')}."
    return nodes

Validation:

Every room has description
Prevents empty desc in Evennia

🧱 MT979 — Integrate prose adjudication pipeline
Modify:

nodes = normalize_prose_seed(nodes)
nodes = assign_atmosphere_tags(nodes)
nodes = refine_prose(nodes)

area_spec["nodes"] = nodes

Validation:

Atmosphere layer active
Descriptions always populated

🧱 MT980 — Full adjudication validation

Confirm:

✔ Nodes have final labels
✔ OCR respected where strong
✔ Weak labels flagged, not blindly used
✔ Edges have valid exit names
✔ Special exits preserved
✔ One-way exits flagged
✔ All rooms have descriptions
✔ Atmosphere tagging applied
✔ AreaSpec updated safely
✔ Pipeline remains deterministic + AI-assisted

🎯 Result After 961–980

You now have:

✔ AI-aware refinement layer (Aedan-in-the-loop)
✔ Structured label adjudication
✔ Exit normalization + command preservation
✔ Atmosphere + prose baseline
✔ Safe, reviewable transformations (no blind mutation)

🧠 Big System Shift

You now moved from:

“parse + label”

to:

extract → interpret → refine → validate → build

🚀 Next Step

👉 981–1000 — Full AreaForge Automation Loop

This will cover:

manifest auto-generation
VS Code workflow integration
one-command “forge area” execution
diffing + rebuild safety

That’s where your “Aedan, build this map” vision becomes fully real.

send 981-1000
Microtasks 981–1000 — AreaForge Automation Loop (Aedan Operator Workflow)

🧱 SECTION 1 — Manifest Automation (981–985)

🧱 MT981 — Create manifest generator
File:
world/area_forge/intake/manifest.py

Add:

import yaml
from pathlib import Path

def create_manifest(map_path, area_id, profile="dr_city"):
    manifest = {
        "area_id": area_id,
        "map_file": str(map_path),
        "profile": profile,
        "generation": {
            "synthesize_street_names": True,
            "synthesize_room_descriptions": True,
            "synthesize_special_exits": True,
        }
    }

    out_path = Path("manifests") / f"{area_id}.yaml"
    out_path.parent.mkdir(parents=True, exist_ok=True)

    with open(out_path, "w") as f:
        yaml.dump(manifest, f)

    return out_path

Validation:

Manifest file is created
Correct YAML structure
Path returned

🧱 MT982 — Add manifest loader
File:
manifest.py

Add:

def load_manifest(path):
    with open(path) as f:
        return yaml.safe_load(f)

Validation:

YAML loads correctly
Returns dict

🧱 MT983 — Add default manifest fallback
File:
CLI or pipeline

Add:

if not manifest_path:
    manifest_path = create_manifest(map_path, area_id)

Validation:

Aedan doesn’t need to manually create manifests
System auto-generates one

🧱 MT984 — Inject manifest into pipeline
Modify pipeline entry:

manifest = load_manifest(manifest_path)

map_path = manifest["map_file"]
area_id = manifest["area_id"]
profile = manifest.get("profile", "dr_city")

Validation:

Pipeline driven by manifest
Centralized config source

🧱 MT985 — Add manifest debug print
Add:

print(f"[AreaForge] Using manifest: {manifest_path}")

Validation:

Aedan sees config source
Easier debugging

🧱 SECTION 2 — Full Pipeline Execution Modes (986–990)

🧱 MT986 — Add pipeline mode system
File:
CLI

Add:

parser.add_argument("--mode", choices=["extract", "enrich", "build", "full"], default="full")

Validation:

Mode selectable from CLI
Default = full

🧱 MT987 — Implement extract mode
Modify pipeline:

if mode == "extract":
    return area_spec

Validation:

Stops after extraction
No enrichment/build occurs

🧱 MT988 — Implement enrich mode

if mode == "enrich":
    area_spec = adjudicate_area_spec(area_spec)
    return area_spec

Validation:

Runs AI/refinement only
No Evennia build

🧱 MT989 — Implement build mode

if mode == "build":
    build_evennia_area(area_spec)
    return

Validation:

Uses pre-built AreaSpec
Does not re-run extraction

🧱 MT990 — Implement full pipeline

if mode == "full":
    area_spec = extract(...)
    area_spec = adjudicate_area_spec(area_spec)
    build_evennia_area(area_spec)

Validation:

Complete pipeline works end-to-end
Single command execution

🧱 SECTION 3 — Build Safety + Diffing (991–995)

🧱 MT991 — Add build snapshot system
File:
world/area_forge/build/snapshots.py

Add:

import json

def save_snapshot(area_id, area_spec):
    path = f"build/snapshots/{area_id}.json"
    with open(path, "w") as f:
        json.dump(area_spec, f, indent=2)

Validation:

Snapshot saved per run
Enables diffing

🧱 MT992 — Add snapshot loader
Add:

def load_snapshot(area_id):
    try:
        with open(f"build/snapshots/{area_id}.json") as f:
            return json.load(f)
    except FileNotFoundError:
        return None

Validation:

Returns None if no snapshot
Safe load

🧱 MT993 — Add diff generator
File:
world/area_forge/build/diff.py

Add:

def diff_area_specs(old, new):
    changes = []

    if not old:
        changes.append("Initial build")
        return changes

    if len(old["nodes"]) != len(new["nodes"]):
        changes.append("Node count changed")

    if len(old["edges"]) != len(new["edges"]):
        changes.append("Edge count changed")

    return changes

Validation:

Detects structural changes
Lightweight comparison

🧱 MT994 — Print diff summary
Add:

old = load_snapshot(area_id)
changes = diff_area_specs(old, area_spec)

for change in changes:
    print(f"[DIFF] {change}")

Validation:

Aedan sees differences before build
Helps prevent accidental rebuild issues

🧱 MT995 — Save snapshot after build
Add:

save_snapshot(area_id, area_spec)

Validation:

Snapshot updated each run
Enables iterative refinement

🧱 SECTION 4 — VS Code / Aedan Workflow Integration (996–1000)

🧱 MT996 — Add top-level runner function
File:
world/area_forge/run.py

Add:

def run_area_forge(map_path, area_id, mode="full"):
    manifest_path = create_manifest(map_path, area_id)
    manifest = load_manifest(manifest_path)

    # pipeline call here

Validation:

Single entrypoint for Aedan
Simplifies CLI usage

🧱 MT997 — Hook CLI to runner
Modify CLI:

from world.area_forge.run import run_area_forge

run_area_forge(args.map, args.area_id, args.mode)

Validation:

CLI uses central runner
No duplicated logic

🧱 MT998 — Add standardized output summary
Add:

print(f"Area: {area_id}")
print(f"Rooms: {len(area_spec['nodes'])}")
print(f"Exits: {len(area_spec['edges'])}")

Validation:

Quick build summary visible
Consistent output

🧱 MT999 — Add artifact output paths
Add:

print("Artifacts:")
print(f" - manifests/{area_id}.yaml")
print(f" - build/areaspec.json")
print(f" - build/review.txt")

Validation:

Aedan knows where to look
Standardized structure

🧱 MT1000 — Full automation validation

Confirm:

✔ Aedan can run one command
✔ Manifest auto-generated
✔ OCR + parsing + enrichment executed
✔ AI adjudication applied
✔ Review + AreaSpec saved
✔ Diff shown before rebuild
✔ Evennia area created/updated
✔ Snapshot saved for future runs
✔ CLI supports extract/enrich/build/full
✔ Output is clear, structured, repeatable

🎯 Final Result (921–1000 Complete)

You now have:

✔ OCR subsystem
✔ Graph extraction + semantic enrichment
✔ AI adjudication layer (Aedan-aware)
✔ Review + validation system
✔ Manifest-driven pipeline
✔ CLI automation
✔ Snapshot + diff safety
✔ One-command area generation

🧠 What You Actually Built

This is no longer a parser.

It is:

An AI-assisted world construction engine with reproducible builds

🚀 What You Can Now Say in VS Code

Aedan, I added maps/xyz.jpg.
Run AreaForge and build area the_landing.

And it is now real, deterministic, and repeatable.