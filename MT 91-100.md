Microtasks 91–100 — Attributes + Balance + Fatigue

(Aedan-ready, no ambiguity, no drift)

Microtask 91 — Add Core Attributes
File:
typeclasses/characters.py
Task:

Add base stats to character creation.

Implementation:

Inside at_object_creation():

self.db.stats = {
    "strength": 10,
    "stamina": 10,
    "agility": 10,
    "reflex": 10,
    "discipline": 10,
    "intelligence": 10,
    "wisdom": 10,
    "charisma": 10,
}
Result:
All characters now have base attributes
Validation:
Reload
Confirm char.db.stats["strength"] == 10
Result:
MT91 PASS
Microtask 92 — Add Balance + Fatigue Pools
File:
typeclasses/characters.py
Task:

Add derived combat resources.

Implementation:

Inside at_object_creation():

self.db.balance = 100
self.db.max_balance = 100

self.db.fatigue = 0
self.db.max_fatigue = 100
Result:
Balance starts full
Fatigue starts empty
Validation:
Reload
Confirm values exist
Result:
MT92 PASS
Microtask 93 — Add Balance + Fatigue Accessors
File:
typeclasses/characters.py
Task:

Add safe getters/setters

Implementation:
def get_balance(self):
    return self.db.balance, self.db.max_balance

def set_balance(self, value):
    self.db.balance = max(0, min(value, self.db.max_balance))

def get_fatigue(self):
    return self.db.fatigue, self.db.max_fatigue

def set_fatigue(self, value):
    self.db.fatigue = max(0, min(value, self.db.max_fatigue))
Result:
Controlled resource management
Result:
MT93 PASS
Microtask 94 — Add Balance Recovery
File:
typeclasses/characters.py
Task:

Balance regenerates over time

Implementation:
def recover_balance(self):
    if self.db.balance < self.db.max_balance:
        self.set_balance(self.db.balance + 2)
Result:
Passive recovery system
Result:
MT94 PASS
Microtask 95 — Add Fatigue Recovery
File:
typeclasses/characters.py
Task:

Fatigue decreases over time

Implementation:
def recover_fatigue(self):
    if self.db.fatigue > 0:
        self.set_fatigue(self.db.fatigue - 2)
Result:
Fatigue drains naturally
Result:
MT95 PASS
Microtask 96 — Hook Recovery Into Global Ticker
File:
server/conf/at_server_startstop.py
Task:

Extend existing ticker loop

Implementation:

Inside ticker:

character.recover_balance()
character.recover_fatigue()
Result:
Continuous system recovery
Validation:
Reload
Values change over time
Result:
MT96 PASS
Microtask 97 — Apply Balance Cost to Attack
File:
commands/cmd_attack.py
Task:

Attacks consume balance

Implementation:

After successful attack:

self.caller.set_balance(self.caller.db.balance - 10)
Result:
Combat now drains balance
Validation:
Attack → balance decreases
Result:
MT97 PASS
Microtask 98 — Apply Fatigue Cost to Attack
File:
commands/cmd_attack.py
Task:

Attacks increase fatigue

Implementation:
self.caller.set_fatigue(self.caller.db.fatigue + 5)
Result:
Combat builds fatigue
Result:
MT98 PASS
Microtask 99 — Add Balance Gate to Attack
File:
commands/cmd_attack.py
Task:

Prevent attacking when off-balance

Implementation:

At top of func() (after RT check):

if self.caller.db.balance <= 0:
    self.caller.msg("You are too off balance to attack.")
    return
Result:
Balance now affects combat viability
Result:
MT99 PASS
Microtask 100 — Extend Stats Command Output
File:
commands/cmd_stats.py
Task:

Expose new systems to player

Implementation:

Add:

bal, max_bal = char.get_balance()
fat, max_fat = char.get_fatigue()

self.caller.msg(f"Balance: {bal}/{max_bal}")
self.caller.msg(f"Fatigue: {fat}/{max_fat}")
Result:
Player can see combat resources
Validation:
stats shows balance + fatigue
Result:
MT100 PASS
🚀 What This Unlocks

After 100, your system becomes:

Attack
→ costs balance
→ builds fatigue
→ recovers over time
→ gated by RT + balance

👉 This is the first true DR combat feel layer