Microtasks 121–130 (Aedan-Ready, No Stop Required)

Execution mode: continuous (only stop on blocker)

Microtask 121 — Add weapon type taxonomy
File modified: typeclasses/weapons.py

Add a central enum-like structure:

WEAPON_TYPES = [
    "brawling",
    "light_edge",
    "heavy_edge",
    "blunt",
    "polearm",
    "short_bow",
    "long_bow",
    "crossbow"
]
No logic change yet
This becomes the canonical list for skills + balance tuning

Result: MT121 PASS

Microtask 122 — Normalize weapon skill mapping
File modified: typeclasses/weapons.py

Ensure every weapon sets:

self.db.skill = "light_edge"  # example

Add validation helper:

def get_weapon_skill(self):
    return self.db.skill or "brawling"

Result: MT122 PASS

Microtask 123 — Route skill usage from attack
File modified: commands/cmd_attack.py

After resolving profile:

skill_name = profile.get("skill", "brawling")
self.caller.use_skill(skill_name)

⚠️ Do NOT remove attack logic yet
→ this is just hooking skill usage into combat

Result: MT123 PASS

Microtask 124 — Add skill data structure expansion
File modified: typeclasses/characters.py

Update skill storage format:

self.db.skills = {
    "brawling": {"rank": 1, "mindstate": 0},
    ...
}

Update learn_skill() to support dict shape.

Result: MT124 PASS

Microtask 125 — Add skill rank getter
File modified: typeclasses/characters.py

Add:

def get_skill_rank(self, skill_name):
    skill = self.db.skills.get(skill_name)
    if not skill:
        return 0
    return skill.get("rank", 0)

Result: MT125 PASS

Microtask 126 — Add skill gain stub (NO progression yet)
File modified: typeclasses/characters.py

Inside use_skill():

if skill_name in self.db.skills:
    self.db.skills[skill_name]["mindstate"] += 1

⚠️ No caps, no pulse yet
→ just accumulating

Result: MT126 PASS

Microtask 127 — Add hit bonus from skill
File modified: commands/cmd_attack.py

Modify accuracy:

skill_rank = self.caller.get_skill_rank(skill_name)
accuracy = 50 + reflex + agility + skill_rank

Result: MT127 PASS

Microtask 128 — Add random body-part targeting
File modified: commands/cmd_attack.py

Add:

location = random.choice(list(target.db.injuries.keys()))

Replace:

target.apply_damage("chest", damage)

with:

target.apply_damage(location, damage)

Result: MT128 PASS

Microtask 129 — Add hit location messaging
File modified: commands/cmd_attack.py

Update messaging:

"You hit {target} in the {location} for {damage} damage."
"{attacker} hits you in the {location} for {damage} damage."

Result: MT129 PASS

Microtask 130 — Full system validation

Validate:

weapon affects:
damage
roundtime
fatigue
skill is:
triggered
mindstate increasing
hit chance affected by skill
random body part works
bleed still applies correctly
NPC loop still stable

Result: MT130 PASS

🔥 What This Batch Achieves

You now have:

✔ Weapon identity (types + skills)
✔ Skill hook into combat
✔ Early learning system (mindstate seed)
✔ Targeted combat (body parts)
✔ Combat variability (location + skill)
🧠 Why This Is the Right Next Step

You just transitioned from:

“combat system”

to:

“combat + progression loop”

That’s the DragonRealms backbone starting to emerge.

⚠️ Important Next Step (Don’t Skip)

After this batch:

👉 131–140 MUST be: Mindstate + Pulse system

Because right now you have:

accumulation ✅
but no conversion ❌

That’s the next critical unlock.