DireMud Batch — Microtasks 51–60 (TASK 6 — HEALING SCAFFOLD, FIXED)
Batch Goal

Introduce stabilization + minor recovery:

stop bleeding
small controlled healing
no full healing system
Global Rules for Aedan

(unchanged, but restated for clarity)

Execute one microtask at a time
Do not skip
Do not combine
Do not broaden scope
On failure:
mark STATUS: BLOCKED
explain why
STOP
Append reports:
## Report — Microtask 51 … 60
🔧 CRITICAL RULE (NEW — PREVENTS YOUR BLOCK)

👉 Any new command MUST be registered in the SAME microtask it becomes usable

Do not defer registration.

Microtask 51 — Add stop_bleeding Method
Objective

Allow bleeding to be cleared.

Instructions

In typeclasses/characters.py:

def stop_bleeding(self, part):
    bp = self.get_body_part(part)
    if not bp:
        return

    bp["bleeding"] = 0
Test Gate
evennia reload
Report Back
method added
reload result
Microtask 52 — Create Tend Command + Register It
Objective

Create and register the command in one step (fixing prior issue)

Instructions

Create file:

commands/cmd_tend.py

from evennia import Command

class CmdTend(Command):
    key = "tend"
REGISTER IT (REQUIRED IN THIS TASK)

Open:
commands/default_cmdsets.py

Add:

from commands.cmd_tend import CmdTend

Inside CharacterCmdSet.at_cmdset_creation():

self.add(CmdTend())
Test Gate

Run:

evennia reload
tend

Expected:

command recognized (no "unknown command")
Report Back
file created
import added
cmdset updated
output observed
Microtask 53 — Add Basic func()
Objective

Ensure command executes

Instructions

In CmdTend:

def func(self):
    self.caller.msg("Tend command received.")
Test Gate
tend

Expected:

Tend command received.
Report Back
output confirmed
Microtask 54 — Parse Body Part
Objective

Accept input

Instructions

Replace func:

def func(self):
    if not self.args:
        self.caller.msg("Tend what?")
        return

    part = self.args.strip().lower()
    self.caller.msg(f"Tending: {part}")
Test Gate
tend head

Expected:

Tending: head
Report Back
parsing confirmed
Microtask 55 — Validate Body Part
Objective

Reject invalid input

Instructions

Add:

bp = self.caller.get_body_part(part)
if not bp:
    self.caller.msg("Invalid body part.")
    return
Test Gate
tend wing

Expected:

Invalid body part.
Report Back
validation confirmed
Microtask 56 — Stop Bleeding via Command
Objective

Connect command → system

Instructions

Add AFTER validation:

self.caller.stop_bleeding(part)
self.caller.msg(f"You stop bleeding on your {part}.")
Test Gate
create bleeding
run:
tend chest
stats

Expected:

bleeding = 0
Report Back
before/after
Microtask 57 — Add heal_body_part Method
Objective

Introduce recovery hook

Instructions

In characters.py:

def heal_body_part(self, part, amount):
    bp = self.get_body_part(part)
    if not bp:
        return

    bp["hp"] = min(bp["max_hp"], bp["hp"] + amount)
Test Gate
reload
Report Back
method added
Microtask 58 — Apply Small Heal in Tend
Objective

Tending slightly restores HP

Instructions

Modify tend:

self.caller.heal_body_part(part, 5)
Test Gate
damage part
run:
tend chest
stats

Expected:

HP increases by 5 (or capped)
Report Back
before/after values
Microtask 59 — Prevent Tend When No Bleeding
Objective

Avoid useless action

Instructions

Add BEFORE stop_bleeding:

if bp["bleeding"] <= 0:
    self.caller.msg("There is no bleeding to tend.")
    return
Test Gate
tend head

(no bleeding)

Expected:

There is no bleeding to tend.
Report Back
output confirmed
Microtask 60 — Add Room Feedback for Tend
Objective

Make actions visible to others (important for multiplayer)

Instructions

Add:

self.caller.location.msg_contents(
    f"{self.caller.key} tends their {part}.",
    exclude=self.caller
)
Test Gate

Two players in room:

tend chest

Expected:

player sees self message
others see:
X tends their chest.
Report Back
both outputs captured