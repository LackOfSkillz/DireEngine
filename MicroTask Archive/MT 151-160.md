Microtask 151 — Add difficulty helper
File modified: typeclasses/characters.py
Add method:
def get_skill_rank(self, skill_name):
    if not self.db.skills:
        return 0
    return self.db.skills.get(skill_name, {}).get("rank", 0)
Purpose: centralize rank lookup for difficulty calculations
Result: MT151 PASS
## Microtask 152 — Add difficulty calculation
File modified: typeclasses/characters.py
Add method:
def calculate_difficulty_ratio(self, skill_name, difficulty):
    rank = self.get_skill_rank(skill_name)
    if difficulty <= 0:
        return 2.0  # treat as trivial
    return rank / difficulty
Result: MT152 PASS
## Microtask 153 — Add difficulty band resolver
File modified: typeclasses/characters.py
Add method:
def get_difficulty_band(self, ratio):
    if ratio > 1.5:
        return "trivial"
    elif ratio > 1.2:
        return "easy"
    elif ratio > 0.8:
        return "optimal"
    elif ratio > 0.5:
        return "hard"
    else:
        return "too_hard"
Result: MT153 PASS
## Microtask 154 — Map band to learning gain
File modified: typeclasses/characters.py
Add method:
def get_learning_gain(self, band):
    return {
        "trivial": 0,
        "easy": 1,
        "optimal": 3,
        "hard": 2,
        "too_hard": 1,
    }.get(band, 0)
Result: MT154 PASS
## Microtask 155 — Add difficulty entry point
File modified: typeclasses/characters.py
Add method:
def get_learning_amount(self, skill_name, difficulty):
    ratio = self.calculate_difficulty_ratio(skill_name, difficulty)
    band = self.get_difficulty_band(ratio)
    return self.get_learning_gain(band), band
Result: MT155 PASS
## Microtask 156 — Integrate difficulty into use_skill
File modified: typeclasses/characters.py
Modify use_skill():
Replace flat gain logic with:
difficulty = kwargs.get("difficulty", 10)

gain, band = self.get_learning_amount(skill_name, difficulty)

if gain <= 0:
    return  # no learning for trivial

skill = self.db.skills.get(skill_name)
if not skill:
    return

skill["mindstate"] = min(
    skill["mindstate"] + gain,
    self.get_mindstate_cap()
)
Result: MT156 PASS
## Microtask 157 — Pass difficulty from attack
File modified: commands/cmd_attack.py
Before calling skill usage, add:
difficulty = target.get_stat("reflex") + target.get_stat("agility")
Then replace:
self.caller.use_skill("brawling")

with:

self.caller.use_skill("brawling", difficulty=difficulty)
Result: MT157 PASS
## Microtask 158 — Add debug output for tuning
File modified: commands/cmd_attack.py
After skill use, add:
self.caller.msg(f"[DEBUG] Learning band: {band}")

👉 You will need to temporarily capture band from use_skill() return
→ Modify use_skill() to optionally return (gain, band)

Result: MT158 PASS
## Microtask 159 — Prevent learning on extreme failure
File modified: commands/cmd_attack.py
In miss branch, DO NOT call use_skill()

(Confirm this remains true after integration)

Result: MT159 PASS
## Microtask 160 — Full validation
Reload Evennia
Run combat tests against:
weak target
equal target
strong target
Confirm:
trivial targets → no learning
equal targets → strong learning
very strong targets → reduced learning
mindstate increases vary per difficulty
no crashes
debug band output matches expectations
Result: MT160 PASS
⚠️ Design Notes (Important)

This system now correctly reproduces DR behavior:

✔ No learning from trivial targets
✔ Peak learning at parity
✔ Reduced learning when overwhelmed
✔ Success-based progression only

🚀 What You Just Unlocked

You now have:

True training curve behavior
Natural anti-grind system
Built-in progression pacing
Skill growth that feels right