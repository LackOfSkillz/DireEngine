DireMud Batch — Microtasks 11–20 (TASK 2)
Batch Goal

Introduce the first functional game loop:

player issues attack <target> → system validates → damage is applied → state updates → feedback is shown

This batch establishes:

command-driven interaction
target resolution
controlled state mutation
observable outcomes

This is the first step where the game becomes interactive.

Global Rules for Aedan

Follow these rules exactly:

Execute one microtask at a time, in order
Do not skip ahead
Do not combine tasks
Do not broaden scope
If a test gate fails:
mark STATUS: BLOCKED
explain why
stop execution
After each microtask, append to report file using exact headings:
## Report — Microtask 11
…
## Report — Microtask 20
Do not introduce:
skills
hit chance
armor
stamina/mana
AI
Do not modify Evennia core files
All validation must be done via:
evennia reload
in-game commands
Report File

Continue using:

diremud_microtasks_1_20_report.md

Append new sections for Microtasks 11–20.

System Constraints for This Batch
Damage is deterministic and fixed
No randomness
No miss chance
No death system yet
No timers
No turn system
Microtask 11 — Create Attack Command File
Objective

Introduce a new command file for attack functionality.

Instructions

Create file:

commands/cmd_attack.py

Add:

from evennia import Command

class CmdAttack(Command):
    key = "attack"
Constraints
Do not add logic yet
Do not import anything else
Test Gate

Run:

evennia reload
Confirm no import errors
Report Back
File created
Class defined
Reload result
Microtask 12 — Add Basic Command Function
Objective

Ensure command executes without error.

Instructions

Inside CmdAttack, add:

def func(self):
    self.caller.msg("Attack command received.")
Constraints
No parsing yet
No logic
Test Gate
Run:

evennia reload

Confirm:
- No import errors
- No syntax errors in cmd_attack.py

Do NOT attempt to run "attack" in-game yet
Report Back
- File modified
- Method added
- Reload result
- Confirmation that command is not yet registered (expected)
Microtask 13 — Register Attack Command
Objective

Make attack command usable.

Instructions

Open:

commands/default_cmdsets.py
Add:
from commands.cmd_attack import CmdAttack
Inside CharacterCmdSet:
self.add(CmdAttack())
Test Gate
Reload server

Run:

attack
Confirm command works
Report Back
File modified
Command registered
Output verified
Microtask 14 — Parse Target Argument
Objective

Extract target from user input.

Instructions

Update func():

def func(self):
    if not self.args:
        self.caller.msg("Attack what?")
        return

    target_name = self.args.strip()
    self.caller.msg(f"Target: {target_name}")
Constraints
Do not resolve objects yet
Test Gate

Run:

attack goblin

Output:

Target: goblin
Report Back
Parsing confirmed
Output verified
Microtask 15 — Resolve Target Object
Objective

Find target in current location.

Instructions

Replace function body:

def func(self):
    if not self.args:
        self.caller.msg("Attack what?")
        return

    target_name = self.args.strip()

    target = self.caller.search(target_name)

    if not target:
        return

    self.caller.msg(f"You target {target.key}.")
Constraints
- Use Evennia `search()` only
- Input to search MUST be stripped of leading/trailing whitespace
- Do not introduce custom lookup logic
- Accept Evennia's default search resolution behavior (no disambiguation handling in this batch)
Test Gate
Reload server:

evennia reload

Ensure another character/object exists in the same room

Run:

attack <name>

Expected:

You target <name>.

Confirm:
- No "Could not find" error
- Target resolves correctly
Report Back
- File modified
- Exact input used for search (after strip)
- Target resolution confirmed
- Exact output captured
Microtask 16 — Prevent Self-Attack
Objective

Disallow attacking self.

Instructions

Add after target resolution:

if target == self.caller:
    self.caller.msg("You cannot attack yourself.")
    return
Test Gate

Run:

attack me

Expected:

You cannot attack yourself.
Report Back
Self-check works
Output verified
Microtask 17 — Set Combat Target
Objective

Activate combat state.

Instructions

Add:

self.caller.set_target(target)
target.set_target(self.caller)

Then:

self.caller.msg(f"You engage {target.key}.")
Constraints
- Combat state should only be set if target passes later validation checks
- Combat engagement in this phase is bidirectional
- Both participants enter in_combat = True
- This is a minimal mutual link, not AI or turn-based combat
Test Gate

Run:

attack <target>

Then:

stats

Expected:

In Combat: True
Target: <target>
Report Back
Combat state verified
Microtask 18 — Apply Fixed Damage
Objective

Introduce damage system.

Instructions

Add:

if not hasattr(target, "set_hp") or target.db.hp is None:
    self.caller.msg(f"You cannot attack {target.key}.")
    return

damage = 10
target.set_hp(target.db.hp - damage)
Constraints
Fixed value only
No randomness
- Do not modify NPC typeclasses in this microtask
- This check is capability validation only, not a full combat-entity system
- Validation must occur BEFORE any state mutation
Test Setup Requirement

- Use only characters created after Microtask 3 was completed
    OR
- Backfill required state on legacy test characters before running the attack test

Required state:
- hp
- max_hp
- in_combat
- target
Test Gate
Run:

evennia reload

Ensure both attacker and target are characters with initialized Task 1 state
Verify target `stats` before attack shows:
HP: 100/100
Target: None
In Combat: False

Run:

attack <target>

Verify target HP decreases by exactly 10
Report Back
- Exact characters used for testing
- Whether they were freshly created or backfilled
- Exact target stats before attack
- Exact target stats after attack
- Exact attack output observed
- Damage applied
- HP change verified
Microtask 19 — Add Combat Messaging
Objective

Provide feedback to both players.

Instructions

Add:

self.caller.msg(f"You hit {target.key} for {damage} damage.")
target.msg(f"{self.caller.key} hits you for {damage} damage.")
Constraints
- Combat messaging must occur only after target validation and damage application succeed
Test Gate
Two-player or test setup
Both messages appear correctly
Report Back
Messaging verified
Microtask 20 — Final Command Flow Validation
Objective

Verify full attack loop works.

Validation Scenario

Player A attacks Player B:

attack B
Expected:
Target set
Combat state true
HP reduced
Messages displayed
Expected Combined Output
You engage B.
You hit B for 10 damage.

Target sees:

A hits you for 10 damage.
Additional Checks
Running stats reflects:
updated HP
correct target
combat active
Test Gate
No errors
All steps verified
Report Back
Full loop validated
Exact outputs captured
Final Batch Requirement

Report file must include:

## Report — Microtask 11
...
## Report — Microtask 20

Each section must include:

exact files changed
exact commands run
exact outputs observed
validation confirmation
Final Guidance for Aedan

This batch is about:

interaction, not complexity

Do not:

add randomness
add combat systems
add cooldowns
add AI

Do:

make the loop work
keep it deterministic
ensure it is observable
After Completion

You will have:

A working game loop:
command → action → state change → feedback