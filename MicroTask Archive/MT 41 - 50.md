Perfect. This is exactly the right level of ambition: **lock the system shape now, keep behavior minimal, stay testable.**

Below is your **next batch**, fully aligned with:

* injury system (body-part based)
* vital vs non-vital logic
* bleeding scaffold (time-based damage)
* zero scope creep into full DR complexity

---

# DireMud Batch — Microtasks 41–50

## Injury + Bleeding System Scaffolding (LOCKED)

---

# Batch Goal

Introduce a **body-part injury system with bleeding**, without expanding into full combat mechanics.

By the end of this batch:

* Damage is stored per body part
* Vital parts are defined
* Bleeding exists and causes passive HP loss
* Players can inspect injuries
* No complex combat effects yet

---

# Global Rules for Aedan

Follow exactly:

1. Execute one microtask at a time, in order

2. Do not skip ahead

3. Do not combine tasks

4. Do not broaden scope

5. If a test gate fails:

   * mark `STATUS: BLOCKED`
   * explain why
   * stop immediately

6. After each microtask, append:

```
## Report — Microtask 41
...
```

7. Do NOT introduce:

* limb penalties
* targeting system
* healing/tend/bandage
* randomness
* AI
* combat effects based on injuries

---

# Report File

Continue:

```
diremud_microtasks_1_50_report.md
```

---

# System Constraints

* HP system remains active
* Injury system is additive (not replacing HP yet)
* Bleeding is deterministic
* No timers beyond simple tick loop
* No player interaction with bleeding yet

---

# Microtask 41 — Add Injury Schema to Character

## Objective

Introduce body-part injury structure.

## Instructions

In:

```
typeclasses/characters.py
```

Inside `at_object_creation`, add:

```python
self.db.injuries = {
    "head":        {"external": 0, "internal": 0, "bleed": 0, "max": 100, "vital": True},
    "chest":       {"external": 0, "internal": 0, "bleed": 0, "max": 120, "vital": True},
    "abdomen":     {"external": 0, "internal": 0, "bleed": 0, "max": 110, "vital": True},
    "back":        {"external": 0, "internal": 0, "bleed": 0, "max": 110, "vital": True},
    "left_arm":    {"external": 0, "internal": 0, "bleed": 0, "max": 80, "vital": False},
    "right_arm":   {"external": 0, "internal": 0, "bleed": 0, "max": 80, "vital": False},
    "left_hand":   {"external": 0, "internal": 0, "bleed": 0, "max": 60, "vital": False},
    "right_hand":  {"external": 0, "internal": 0, "bleed": 0, "max": 60, "vital": False},
    "left_leg":    {"external": 0, "internal": 0, "bleed": 0, "max": 90, "vital": False},
    "right_leg":   {"external": 0, "internal": 0, "bleed": 0, "max": 90, "vital": False},
}
```

## Test Gate

```
evennia reload
```

Create new character → no errors

## Report Back

* Schema added
* Reload success
* Character creation verified

---

# Microtask 42 — Add apply_damage Method

## Objective

Create central damage handler.

## Instructions

Add:

```python
def apply_damage(self, location, amount):
    if location not in self.db.injuries:
        return

    self.db.injuries[location]["external"] += amount
```

## Test Gate

```
evennia reload
```

No errors

## Report Back

* Method added
* Reload success

---

# Microtask 43 — Integrate Injury into Attack

## Objective

Connect combat to injury system.

## Instructions

In `cmd_attack.py`, AFTER HP damage:

```python
target.apply_damage("chest", damage)
```

## Constraints

* Always "chest"
* No randomness

## Test Gate

```
attack <target>
```

No errors

## Report Back

* File modified
* Attack works
* No crash

---

# Microtask 44 — Add Injury Severity Helper

## Objective

Translate numeric damage into readable states.

## Instructions

Add:

```python
def get_injury_level(self, value):
    if value == 0:
        return "none"
    elif value <= 10:
        return "light"
    elif value <= 25:
        return "moderate"
    elif value <= 50:
        return "severe"
    else:
        return "critical"
```

## Test Gate

Reload only

## Report Back

* Method added
* Reload success

---

# Microtask 45 — Create Injuries Command

## Objective

Expose injury data to player.

## Instructions

Create:

```
commands/cmd_injuries.py
```

Add basic command class.

## Test Gate

Reload

## Report Back

* File created
* No errors

---

# Microtask 46 — Implement Injuries Output

## Objective

Display injuries.

## Instructions

Loop through:

```python
for part, data in self.caller.db.injuries.items():
```

Output:

```
Head: light
Chest: moderate
```

Use `get_injury_level`.

## Test Gate

```
injuries
```

## Report Back

* Output verified

---

# Microtask 47 — Add Bleed Generation

## Objective

Damage can cause bleeding.

## Instructions

Modify `apply_damage`:

```python
if amount >= 10:
    self.db.injuries[location]["bleed"] += 1
```

## Test Gate

Attack target multiple times
Check internal values via shell

## Report Back

* Bleed increases confirmed

---

# Microtask 48 — Add Bleed Tick Handler

## Objective

Bleeding causes passive HP loss.

## Instructions

Add method:

```python
def process_bleed(self):
    total_bleed = sum(part["bleed"] for part in self.db.injuries.values())

    if total_bleed > 0:
        self.set_hp(self.db.hp - total_bleed)
```

## Test Gate

Manual call via shell

## Report Back

* HP reduction confirmed

---

# Microtask 49 — Hook Bleed Processing

## Objective

Run bleed over time.

## Instructions

Use simple hook:

```python
def at_tick(self):
    self.process_bleed()
```

(Or equivalent existing Evennia ticker)

## Constraints

* No complex scheduler
* Minimal working hook only

## Test Gate

Observe HP drop over time

## Report Back

* Bleed ticking confirmed

---

# Microtask 50 — Add Vital Destruction Check

## Objective

Detect fatal injuries.

## Instructions

Add:

```python
def is_vital_destroyed(self):
    for part, data in self.db.injuries.items():
        if data["vital"] and data["external"] >= data["max"]:
            return True
    return False
```

Then in `apply_damage`:

```python
if self.is_vital_destroyed():
    self.db.is_dead = True
```

## Test Gate

Manually set:

```
head external >= max
```

Check:

```
self.db.is_dead == True
```

## Report Back

* Fatal condition confirmed

---

# Final Batch Outcome

After MT50, you now have:

✅ Body-part injury system
✅ Vital vs non-vital structure
✅ Bleeding system (passive damage)
✅ Combat integration
✅ Injury visibility command
✅ Death condition scaffold

---

# 🔥 What you just built (important)

You now have the **foundation of DragonRealms combat**, without the complexity.

Everything else becomes additive:

* retreat system → uses legs
* weapon penalties → use hands/arms
* aiming → head/eyes
* healing → target body parts
* bleeding control → future batch

---

