This first block is Profession Foundation (P1) only.

👉 No abilities
👉 No combat changes
👉 No progression yet

Just:

profession exists
character can join one
system recognizes it
🧱 MICRO TASKS (3000–3019) — PROFESSION FOUNDATION (P1)
🟫 CORE DATA MODEL
3000

Create new file:

world/professions/professions.py
3001

Define base profession registry:

PROFESSIONS = {}
3002

Create registration function:

def register_profession(key, data):
    PROFESSIONS[key] = data
3003

Define Commoner (default):

register_profession("commoner", {
    "name": "Commoner",
    "skill_weights": {},
    "description": "Untrained and unguilded."
})
3004

Add first 4 starter professions (data only, no logic):

barbarian
thief
empath
mage

Each includes:

{
    "name": "...",
    "skill_weights": {},
    "description": "...",
    "primary": "...",
    "secondary": [],
    "tertiary": []
}
🟩 CHARACTER INTEGRATION
3005

In typeclasses/characters.py, add:

self.db.profession = "commoner"

Ensure default on creation

3006

Create helper:

def get_profession(self):
    from world.professions.professions import PROFESSIONS
    return PROFESSIONS.get(self.db.profession, PROFESSIONS["commoner"])
3007

Create setter:

def set_profession(self, key):
    from world.professions.professions import PROFESSIONS

    if key not in PROFESSIONS:
        return False

    self.db.profession = key
    return True
🟨 COMMAND LAYER
3008

Create command file:

commands/cmd_profession.py
3009

Add command:

profession

Displays:

current profession
description
3010

Output format:

You are a Barbarian.
A brutal warrior who...
🟦 JOIN SYSTEM (BASIC, NO QUESTS YET)
3011

Add command:

join <profession>
3012

Validate profession exists:

if profession not in PROFESSIONS:
    self.msg("That profession does not exist.")
3013

Prevent rejoining same profession:

if self.db.profession == profession:
    self.msg("You are already that profession.")
3014

Set profession:

self.caller.set_profession(profession)
3015

Confirm to player:

You are now recognized as a Thief.
🟪 BASIC SYSTEM HOOKS
3016

On profession change, send state update:

self.msg(text="", type="character", profession=profession)
3017

Update stats command to include:

Profession: Thief
3018

Update debug panel (if applicable) to show:

profession: thief
🟧 SAFETY / DEFAULTS
3019

Ensure all characters fallback safely:

if not self.db.profession:
    self.db.profession = "commoner"
🧠 RULES FOR AEDAN (LOCK THIS)
NO abilities yet
NO skill modifiers yet
NO combat hooks
NO progression logic
NO guild halls

👉 This is ONLY identity + plumbing

🚀 What Success Looks Like After 3019
Every character has a profession (default: commoner)
Player can:
profession
join barbarian
System recognizes profession everywhere
UI updates when profession changes
🔥 Why This Matters

You just created:

A stable identity layer

Everything else (skills, abilities, progression) plugs into this.

MICRO TASKS (3020–3039) — SKILLSET WEIGHTING
🟫 SKILLSET FOUNDATION
3020

Create new file:

world/skills/skillsets.py
3021

Define core DR-inspired skillsets:

SKILLSETS = [
    "weapon",
    "armor",
    "survival",
    "magic",
    "lore"
]
3022

Create helper:

def is_valid_skillset(name):
    return name in SKILLSETS
🟩 PROFESSION SKILL WEIGHTS
3023

Update each profession in professions.py:

Add:

"skill_weights": {
    "weapon": 1.0,
    "armor": 0.7,
    "survival": 0.5,
    "magic": 0.2,
    "lore": 0.3
}
3024

Define standard tiers:

primary → 1.0
secondary → 0.7
tertiary → 0.4
3025

Apply weights correctly to:

barbarian
thief
empath
mage

(no need for perfection yet—just correct structure)

🟨 CHARACTER SKILL ACCESS
3026

In Character, add:

def get_skill_weight(self, skillset):
    prof = self.get_profession()
    return prof.get("skill_weights", {}).get(skillset, 0.5)
3027

Fallback behavior:

If missing → return 0.5

🟦 XP HOOK (CORE CHANGE)
3028

Locate your XP gain function (or create one):

world/systems/skills.py
3029

Modify XP gain:

def apply_xp(character, skillset, amount):
    weight = character.get_skill_weight(skillset)
    adjusted = amount * weight
3030

Apply adjusted XP instead of raw XP

🟥 TEST HOOK COMMAND
3031

Create test command:

train <skillset>
3032

Command calls:

apply_xp(self.caller, skillset, 10)
3033

Output:

You train weapon skill.
🟪 FEEDBACK (IMPORTANT FOR PLAYER TRUST)
3034

Show learning rate difference:

self.msg(f"You learn ({skillset}) at {int(weight*100)}% efficiency.")
3035

Add to debug panel:

"xp_gain": adjusted
🟩 STATS INTEGRATION
3036

Update stats command:

Add:

Skill Weights:
Weapon: 100%
Armor: 70%
...
3037

Format cleanly (no spam)

🟨 VALIDATION
3038

Prevent invalid skillset training:

if not is_valid_skillset(skillset):
    self.msg("Unknown skillset.")
🟧 SAFETY
3039

Ensure XP never goes negative or zero:

adjusted = max(1, adjusted)
🧠 RULES FOR AEDAN (REINFORCE HARD)
No abilities
No skill trees
No per-skill complexity
No combat modifiers yet
No progression gates yet

👉 ONLY:

skillsets
weights
XP scaling
🚀 What Success Looks Like After 3039
Barbarian trains weapon faster than magic
Mage trains magic faster than weapon
Thief excels in survival
Empath leans lore/survival

👉 You can FEEL profession difference immediately

🔥 What You Just Built

Not professions yet.

You built:

A learning bias system

That is the core of DragonRealms.

MICRO TASKS (3040–3059) — SIGNATURE SUBSYSTEMS (P3)
🟫 CORE SUBSYSTEM FRAMEWORK
3040

Create new file:

world/professions/subsystems.py
3041

Define base subsystem class:

class ProfessionSubsystem:
    def __init__(self, character):
        self.character = character

    def tick(self):
        pass

    def get_state(self):
        return {}
3042

Add Character hook:

def get_subsystem(self):
    if not hasattr(self.ndb, "subsystem"):
        from world.professions.subsystems import create_subsystem
        self.ndb.subsystem = create_subsystem(self)
    return self.ndb.subsystem
3043

Create factory:

def create_subsystem(character):
    prof = character.db.profession

    if prof == "barbarian":
        return BarbarianSubsystem(character)
    if prof == "thief":
        return ThiefSubsystem(character)
    if prof == "empath":
        return EmpathSubsystem(character)
    if prof == "mage":
        return MageSubsystem(character)

    return ProfessionSubsystem(character)
🟩 BARBARIAN — RESOURCE SYSTEM (INNER FIRE LITE)
3044

Create class:

class BarbarianSubsystem(ProfessionSubsystem):
    def __init__(self, character):
        super().__init__(character)
        self.fire = 0
        self.max_fire = 100
3045

Add gain method:

def gain_fire(self, amount):
    self.fire = min(self.max_fire, self.fire + amount)
3046

Add decay:

def tick(self):
    self.fire = max(0, self.fire - 1)
🟨 THIEF — FOCUS SYSTEM
3047

Create class:

class ThiefSubsystem(ProfessionSubsystem):
    def __init__(self, character):
        super().__init__(character)
        self.focus = 100
        self.max_focus = 100
3048

Add drain:

def use_focus(self, amount):
    if self.focus < amount:
        return False
    self.focus -= amount
    return True
3049

Add regen:

def tick(self):
    self.focus = min(self.max_focus, self.focus + 1)
🟦 EMPATH — LINK/TRANSFER POOL
3050

Create class:

class EmpathSubsystem(ProfessionSubsystem):
    def __init__(self, character):
        super().__init__(character)
        self.transfer_pool = 0
        self.max_pool = 100
3051

Add absorb:

def absorb(self, amount):
    self.transfer_pool = min(self.max_pool, self.transfer_pool + amount)
3052

Add bleed:

def tick(self):
    self.transfer_pool = max(0, self.transfer_pool - 1)
🟥 MAGE — ATTUNEMENT / CHARGE
3053

Create class:

class MageSubsystem(ProfessionSubsystem):
    def __init__(self, character):
        super().__init__(character)
        self.attunement = 0
        self.max_attunement = 100
3054

Add charge:

def charge(self, amount):
    self.attunement = min(self.max_attunement, self.attunement + amount)
3055

Add decay:

def tick(self):
    self.attunement = max(0, self.attunement - 1)
🟪 GLOBAL TICK HOOK
3056

Hook into your pulse system:

(wherever your tick loop exists)

char.get_subsystem().tick()
3057

Ensure safe call:

if hasattr(char, "get_subsystem"):
🟩 STATE OUTPUT (CRITICAL FOR UI)
3058

Add to get_state() per subsystem:

Example:

def get_state(self):
    return {
        "type": "barbarian",
        "fire": self.fire,
        "max_fire": self.max_fire
    }
3059

Send state to client:

self.msg(text="", type="subsystem", **self.get_subsystem().get_state())
🧠 RULES FOR AEDAN (LOCK THIS HARD)
NO abilities yet
NO combat integration
NO balance tuning
NO scaling curves
NO UI styling beyond display

👉 ONLY:

resource exists
resource changes over time
resource syncs to client
🚀 What Success Looks Like After 3059
Barbarian has a fire meter
Thief has a focus meter
Empath has a transfer pool
Mage has attunement

👉 All:

tick over time
update live
visible in UI
🔥 What You Just Built

This is the core identity engine.

Not:

skills
not abilities
not combat

This is:

“What makes this profession feel different moment-to-moment”

MICRO TASKS (3060–3079) — FIRST ABILITIES
🟫 ABILITY FRAMEWORK (MINIMAL, NOT A SYSTEM YET)
3060

Create file:

world/professions/abilities.py
3061

Create base class:

class Ability:
    def __init__(self, character):
        self.character = character

    def can_use(self):
        return True

    def use(self):
        pass
3062

Add helper on Character:

def use_ability(self, ability_cls):
    ability = ability_cls(self)

    if not ability.can_use():
        return False

    ability.use()
    return True
🟩 BARBARIAN ABILITIES
3063 — Berserk (resource builder)
class Berserk(Ability):
    def can_use(self):
        return True

    def use(self):
        sub = self.character.get_subsystem()
        sub.gain_fire(20)

        self.character.msg("You surge into a berserk rage!")
3064 — Roar (resource spender)
class Roar(Ability):
    def can_use(self):
        return self.character.get_subsystem().fire >= 20

    def use(self):
        sub = self.character.get_subsystem()
        sub.fire -= 20

        self.character.msg("You unleash a terrifying roar!")
🟨 THIEF ABILITIES
3065 — Focus (regen boost)
class Focus(Ability):
    def use(self):
        sub = self.character.get_subsystem()
        sub.focus = min(sub.max_focus, sub.focus + 20)

        self.character.msg("You steady your focus.")
3066 — Slip (resource use)
class Slip(Ability):
    def can_use(self):
        return self.character.get_subsystem().focus >= 15

    def use(self):
        sub = self.character.get_subsystem()
        sub.use_focus(15)

        self.character.msg("You slip through shadows.")
🟦 EMPATH ABILITIES
3067 — Diagnose
class Diagnose(Ability):
    def use(self):
        self.character.msg("You assess your condition.")
3068 — Transfer
class Transfer(Ability):
    def can_use(self):
        return True

    def use(self):
        sub = self.character.get_subsystem()
        sub.absorb(15)

        self.character.msg("You draw in another's pain.")
🟥 MAGE ABILITIES
3069 — Charge
class Charge(Ability):
    def use(self):
        sub = self.character.get_subsystem()
        sub.charge(20)

        self.character.msg("You gather magical energy.")
3070 — Cast Bolt
class CastBolt(Ability):
    def can_use(self):
        return self.character.get_subsystem().attunement >= 20

    def use(self):
        sub = self.character.get_subsystem()
        sub.attunement -= 20

        self.character.msg("You release a bolt of energy!")
🟪 COMMAND HOOKS
3071

Create command:

ability <name>
3072

Map names to classes:

ABILITY_MAP = {
    "berserk": Berserk,
    "roar": Roar,
    "focus": Focus,
    "slip": Slip,
    "diagnose": Diagnose,
    "transfer": Transfer,
    "charge": Charge,
    "bolt": CastBolt
}
3073

Execute:

ability_cls = ABILITY_MAP.get(name)

if not ability_cls:
    self.msg("Unknown ability.")
    return

self.caller.use_ability(ability_cls)
🟩 PROFESSION RESTRICTION
3074

Add guard:

if self.caller.db.profession != expected_profession:
    self.msg("You cannot use that ability.")

(Simple check per ability)

🟨 UI STATE UPDATE
3075

After ability use:

self.msg(text="", type="subsystem", **self.get_subsystem().get_state())
🟦 FEEDBACK IMPROVEMENT
3076

Add failure message:

if not ability.can_use():
    self.msg("You lack the energy to do that.")
🟥 DEBUG / VISIBILITY
3077

Log ability usage:

print(f"{self.character} used {self.__class__.__name__}")
🟧 SAFETY
3078

Ensure subsystem exists before use:

sub = self.character.get_subsystem()
if not sub:
    return
🟫 FINAL VALIDATION
3079

Test all professions:

Barbarian → berserk / roar
Thief → focus / slip
Empath → diagnose / transfer
Mage → charge / bolt

Ensure:

resource changes
messages show
UI updates
🧠 RULES (CRITICAL)
No cooldown system yet
No damage calculations
No combat hooks
No scaling formulas
No ability trees

👉 This is proof of system, not balance

🚀 What You Have After 3079

Each profession can now:

use abilities
consume/generate resources
feel mechanically different
🔥 What You Just Built

This is your first:

Vertical slice of profession gameplay

Not theoretical. Not future. Playable.

P5 — PROGRESSION + UNLOCKS

👉 Abilities are no longer free
👉 Professions now gate advancement
👉 Players must earn their kit

Still:

no deep trees
no grind tuning
no quest chains yet
🧱 MICRO TASKS (3080–3099) — PROGRESSION + UNLOCKS
🟫 ABILITY REQUIREMENTS (DATA)
3080

Update each ability class to include:

required_rank = 1
3081

Add to Character:

self.db.profession_rank = 1

(default on creation)

3082

Add getter:

def get_profession_rank(self):
    return self.db.profession_rank or 1
🟩 ABILITY LOCKING
3083

Update can_use() in base Ability:

def can_use(self):
    if self.character.get_profession_rank() < self.required_rank:
        return False
    return True
3084

Add failure message:

if self.character.get_profession_rank() < self.required_rank:
    self.character.msg("You are not experienced enough to use that.")
🟨 ASSIGN RANKS TO ABILITIES
3085

Set:

basic abilities → rank 1
second abilities → rank 2

Example:

class Roar(Ability):
    required_rank = 2
3086

Apply to all professions:

Berserk → rank 1
Roar → rank 2
Focus → rank 1
Slip → rank 2
Diagnose → rank 1
Transfer → rank 2
Charge → rank 1
Bolt → rank 2
🟦 RANK ADVANCEMENT SYSTEM (MINIMAL)
3087

Create function:

def advance_profession(self):
    self.db.profession_rank += 1
3088

Add max cap (temporary):

MAX_RANK = 10
3089

Prevent overflow:

if self.db.profession_rank >= MAX_RANK:
    return False
🟥 ADVANCEMENT COMMAND
3090

Create command:

advance
3091

Basic logic:

self.caller.advance_profession()
3092

Output:

You advance in your profession.
🟪 PROGRESSION HOOK (SKILL-BASED GATE)
3093

Add requirement check (placeholder):

if self.caller.db.total_xp < required_xp:
    self.msg("You are not ready to advance.")
3094

Define placeholder XP requirement:

required_xp = self.caller.get_profession_rank() * 100
🟩 FEEDBACK + VISIBILITY
3095

Update profession command:

Profession: Thief
Rank: 2
3096

Update stats:

Profession Rank: 2
🟨 UI STATE UPDATE
3097

Send update:

self.msg(text="", type="character", profession_rank=self.db.profession_rank)
🟦 DEBUG / VALIDATION
3098

Log advancement:

print(f"{self} advanced to rank {self.db.profession_rank}")
🟥 SAFETY
3099

Ensure profession exists before advancing:

if not self.db.profession:
    return
🧠 RULES FOR AEDAN (VERY IMPORTANT)
No branching trees
No trainers yet
No quests
No skill checks tied to specific skills yet
No specialization paths

👉 This is:

rank gate
ability unlock
progression loop
🚀 What You Have After 3099

Players now:

start at rank 1
unlock abilities as they advance
cannot use higher-tier abilities early
feel progression immediately
🔥 What You Just Built

This is:

The commitment layer

Before:

professions = flavor

Now:

professions = investment

P6 — WORLD INTEGRATION (GUILDS + TRAINERS)

👉 Professions are no longer menu choices
👉 They exist in locations
👉 Advancement requires presence
👉 Identity becomes institutional

Still controlled:

no quest chains yet
no faction politics yet
no reputation system yet
🧱 MICRO TASKS (3100–3119) — GUILDS + TRAINERS
🟫 GUILD DATA LAYER
3100

Create file:

world/professions/guilds.py
3101

Define guild registry:

GUILDS = {}
3102

Add registration function:

def register_guild(key, data):
    GUILDS[key] = data
3103

Define starter guilds:

barbarian_guild
thief_guild
empath_guild
mage_guild

Each includes:

{
    "profession": "barbarian",
    "name": "Barbarian Guild",
    "trainer_npc": "barbarian_trainer",
    "location_tag": "barbarian_guildhall"
}
🟩 LOCATION HOOK
3104

Add to room typeclass:

self.db.guild_tag = None
3105

Assign guild tags to rooms:

Example:

room.db.guild_tag = "barbarian_guildhall"
🟨 TRAINER NPC
3106

Create NPC type:

typeclasses/npcs/guild_trainer.py
3107

Add attributes:

self.db.guild = "barbarian_guild"
3108

Add helper:

def can_train(self, character):
    return character.db.profession == GUILDS[self.db.guild]["profession"]
🟦 JOIN RESTRICTION (LOCATION-BASED)
3109

Update join command:

Require player in correct guildhall:

if caller.location.db.guild_tag != expected_tag:
    self.msg("You must be at the guildhall to join.")
3110

Prevent joining wrong guild:

if guild["profession"] != requested_profession:
    deny
🟥 ADVANCE RESTRICTION (TRAINER REQUIRED)
3111

Update advance command:

Check for trainer in room:

trainer = next(obj for obj in room.contents if obj.db.guild == caller.db.profession)
3112

Fail if none:

self.msg("You must seek a guild trainer to advance.")
🟪 TRAIN COMMAND
3113

Create command:

train
3114

Validate trainer present:

if not trainer.can_train(self.caller):
    self.msg("This trainer cannot teach you.")
3115

Advance via trainer:

self.caller.advance_profession()
3116

Output:

The trainer instructs you in your path.
You advance to rank X.
🟩 WORLD FEEDBACK
3117

On join:

self.msg("You are accepted into the guild.")
3118

On wrong guild:

self.msg("You do not belong here.")
🟨 SAFETY
3119

Prevent advancing outside guild:

if caller.location.db.guild_tag != correct_tag:
    deny
🧠 RULES FOR AEDAN (LOCK HARD)
No quest requirements yet
No faction alignment
No reputation system
No multi-trainer complexity
No cross-guild interactions

👉 ONLY:

guild exists
trainer exists
location matters
advancement requires presence
🚀 What You Have After 3119

Players must now:

go to a guildhall
join profession there
return to trainer to advance
exist physically in the world
🔥 What You Just Built

This is:

Institutional identity

Before:

profession = menu choice

Now:

profession = place + people + access


P7 — PROFESSION CONSEQUENCES (REACTION + ACCESS)

👉 The world now reacts differently based on profession
👉 Not full reputation system yet—just clear, visible consequences

Still controlled:

no full crime system yet
no faction politics yet
no guards chasing you (that’s later)
🧱 MICRO TASKS (3120–3139) — WORLD REACTION LAYER
🟫 PROFESSION TAGGING (WORLD AWARENESS)
3120

Add helper on Character:

def get_profession_key(self):
    return self.db.profession or "commoner"
3121

Add shorthand:

def is_profession(self, key):
    return self.get_profession_key() == key
🟩 NPC REACTION SYSTEM (LIGHT)
3122

In NPC base class, add:

def react_to(self, character):
    return "neutral"
3123

Extend with simple logic:

if character.is_profession("thief"):
    return "suspicious"

if character.is_profession("empath"):
    return "trusting"

if character.is_profession("mage"):
    return "wary"
3124

Add default output hook:

def at_object_receive(self, character, **kwargs):
    reaction = self.react_to(character)

    if reaction == "suspicious":
        self.msg_contents(f"{self.key} eyes {character.key} suspiciously.")
🟨 ROOM ACCESS CONTROL (LIGHT GATING)
3125

Add to Room:

self.db.allowed_professions = None
3126

On movement:

if room.db.allowed_professions:
    if caller.get_profession_key() not in room.db.allowed_professions:
        deny entry
3127

Add fail message:

"You are not permitted to enter here."
🟦 SHOP / SERVICE RESTRICTIONS
3128

In shop NPC:

def can_trade(self, character):
    if character.is_profession("thief"):
        return False
    return True
3129

On deny:

"The shopkeeper refuses to deal with you."
🟥 MAGIC VISIBILITY (BASIC HOOK)
3130

When mage uses ability:

room.msg_contents(f"{character.key} radiates magical energy.")
🟪 STEALTH CONSEQUENCE (THIEF SIGNAL)
3131

On thief ability use:

room.msg_contents(f"{character.key} moves with unsettling subtlety.")
🟩 EMPATH TRUST SIGNAL
3132

On empath presence:

room.msg_contents(f"{character.key} carries a calming presence.")
🟨 BARBARIAN PRESENCE
3133

On entry:

room.msg_contents(f"{character.key} exudes raw physical power.")
🟦 COMMAND FEEDBACK (PLAYER AWARENESS)
3134

Add to stats:

Social Standing: Neutral / Suspicious / Trusted
🟥 DEBUG OUTPUT
3135

Log reactions:

print(f"{npc} reacts to {character} as {reaction}")
🟪 CONSISTENCY HOOK
3136

Ensure reactions fire on:

room entry
interaction
ability use
🟩 OPTIONAL LIGHT FLAGGING
3137

Add temp flag:

character.db.last_seen_magic = True
3138

Decay flag over time (simple tick)

🟨 SAFETY
3139

Ensure no hard locks:

always allow movement fallback unless explicitly restricted
never soft-lock player
🧠 RULES FOR AEDAN (CRITICAL)
No guard AI yet
No arrest system
No reputation tracking
No permanent penalties

👉 This is:

flavor → system bridge
visible consequence without punishment
🚀 What You Have After 3139
NPCs react differently
rooms can restrict access
shops can deny service
professions visibly affect the world
🔥 What You Just Built

This is:

The first layer of world bias

Before:

world = neutral container

Now:

world = responsive environment

P8 — CRIME + AWARENESS SYSTEM (FOUNDATION)

This is NOT full crime yet.
This is the mechanical backbone.

🧱 MICRO TASKS (3140–3159) — STEALTH vs AWARENESS
🟫 AWARENESS STATE (CHARACTER)
3140

Add to Character:

self.db.awareness = 0
self.db.max_awareness = 100
3141

Add helper:

def get_awareness(self):
    return self.db.awareness or 0
3142

Add regen (tick hook):

self.db.awareness = min(self.db.max_awareness, self.db.awareness + 1)
🟩 STEALTH STATE
3143

Add to Character:

self.db.hidden = False
3144

Add helper:

def is_hidden(self):
    return self.db.hidden
🟨 HIDE COMMAND
3145

Create command:

hide
3146

On use:

self.caller.db.hidden = True
self.msg("You attempt to blend into the surroundings.")
🟦 REVEAL MECHANIC
3147

Add reveal function:

def reveal(self):
    self.db.hidden = False
    self.msg("You are revealed.")
🟥 DETECTION CHECK (CORE)
3148

Create function:

import random

def detection_check(observer, target):
    stealth = 50 if target.is_hidden() else 0
    awareness = observer.get_awareness()

    roll = random.randint(1, 100)

    return roll + awareness > stealth + 50
🟪 ROOM VISIBILITY FILTER
3149

In room display:

Skip hidden players unless detected:

if obj.is_hidden():
    if not detection_check(looker, obj):
        continue
🟩 PASSIVE DETECTION
3150

On room entry:

for other in room.contents:
    if other.is_hidden():
        if detection_check(self, other):
            other.reveal()
🟨 SEARCH COMMAND
3151

Create command:

search
3152

Increase awareness:

self.caller.db.awareness += 20
self.msg("You search carefully.")
🟦 STEALTH DECAY
3153

On movement:

self.db.hidden = False
🟥 THIEF BONUS
3154

Modify detection:

if target.is_profession("thief"):
    stealth += 20
🟪 EMPATH BONUS (PERCEPTION LEAN)
3155

Modify awareness:

if observer.is_profession("empath"):
    awareness += 10
🟩 FEEDBACK
3156

On successful hide:

You fade from obvious sight.
3157

On failed detection:

(no message — important for tension)

3158

On detection success:

You notice someone hiding nearby.
🟨 SAFETY
3159

Ensure hidden players cannot be permanently invisible:

always reveal on:
movement
ability use
attack
🧠 RULES FOR AEDAN (VERY IMPORTANT)
No stealing yet
No inventory interaction
No guards
No crime flags
No punishment systems

👉 This is ONLY:

hide
detect
reveal
🚀 What You Have After 3159

Players can now:

hide
move unseen (briefly)
be detected
actively search

👉 This is the core DR tension loop

🔥 What You Just Built

This is:

The first real PvE/PvP interaction system

Even without combat:

players affect each other
presence matters
awareness matters