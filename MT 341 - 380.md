Microtasks 341–360 — Ranged Combat + Perception Integration
🧱 MT341 — Add distance tracking to combat

File: typeclasses/characters.py

Add:
self.db.combat_range = {}  # target -> distance
Helper:
def get_range(self, target):
    return self.db.combat_range.get(target.id, "melee")
Validation:
Default range = melee

Result: MT341 PASS

🧱 MT342 — Add set_range method
def set_range(self, target, value):
    self.db.combat_range[target.id] = value
Validation:
Range updates correctly

Result: MT342 PASS

🧱 MT343 — Define range bands
RANGE_BANDS = ["melee", "reach", "missile"]
Validation:
Enum exists

Result: MT343 PASS

🧱 MT344 — Add advance command

File: commands/cmd_advance.py

class CmdAdvance(Command):
    key = "advance"

    def func(self):
        target = self.caller.db.target
        if not target:
            self.caller.msg("Advance toward what?")
            return

        self.caller.set_range(target, "melee")
        self.caller.msg(f"You advance on {target.key}.")
Validation:
Range becomes melee

Result: MT344 PASS

🧱 MT345 — Add retreat command

File: commands/cmd_retreat.py

class CmdRetreat(Command):
    key = "retreat"

    def func(self):
        target = self.caller.db.target
        if not target:
            return

        self.caller.set_range(target, "missile")
        self.caller.msg(f"You retreat from {target.key}.")
Validation:
Range becomes missile

Result: MT345 PASS

🧱 MT346 — Restrict melee attacks by range

cmd_attack.py

if self.caller.get_range(target) != "melee":
    self.caller.msg("You are too far away to attack in melee.")
    return
Validation:
Cannot melee from range

Result: MT346 PASS

🧱 MT347 — Add ranged weapon flag

typeclasses/weapons.py

self.db.is_ranged = False
Validation:
Flag exists

Result: MT347 PASS

🧱 MT348 — Add ranged weapon types

Examples:

bow.db.is_ranged = True
bow.db.weapon_profile["range"] = "missile"
Validation:
Ranged weapons defined

Result: MT348 PASS

🧱 MT349 — Allow ranged attacks outside melee
if weapon.db.is_ranged:
    if self.caller.get_range(target) == "melee":
        accuracy -= 20
Validation:
Penalty at melee

Result: MT349 PASS

🧱 MT350 — Add aiming system

characters.py

self.db.aiming = None
Validation:
Field exists

Result: MT350 PASS

🧱 MT351 — Create aim command
class CmdAim(Command):
    key = "aim"

    def func(self):
        target = self.caller.db.target
        if not target:
            return

        self.caller.db.aiming = target.id
        self.caller.msg(f"You begin aiming at {target.key}.")
Validation:
Aim state stored

Result: MT351 PASS

🧱 MT352 — Apply aim bonus
if self.caller.db.aiming == target.id:
    accuracy += 15
Validation:
Aimed attacks more accurate

Result: MT352 PASS

🧱 MT353 — Clear aim after attack
self.caller.db.aiming = None
Validation:
Aim resets

Result: MT353 PASS

🧱 MT354 — Add perception stat usage
def get_perception(self):
    return self.db.stats.get("intelligence", 10)
Validation:
Returns value

Result: MT354 PASS

🧱 MT355 — Add stealth stat stub
def get_stealth(self):
    return self.get_skill("stealth")
Validation:
Returns value

Result: MT355 PASS

🧱 MT356 — Implement perception vs stealth check
def can_detect(self, target):
    return self.get_perception() >= target.get_stealth()
Validation:
Simple compare works

Result: MT356 PASS

🧱 MT357 — Hide undetected targets from look
if not self.can_detect(obj):
    continue
Validation:
Hidden targets not shown

Result: MT357 PASS

🧱 MT358 — Add ranged messaging
"You fire at {target} from a distance."
Validation:
Distinct from melee

Result: MT358 PASS

🧱 MT359 — Add sneak movement messaging
"You move quietly to the north."

(observer version only if detected)

Validation:
Conditional visibility

Result: MT359 PASS

🧱 MT360 — Full system validation

Confirm:

melee vs ranged works
distance affects combat
aim increases accuracy
stealth hides characters
perception reveals them
no crashes
🎯 Outcome

After this:

✔ Distance exists
✔ Ranged combat works
✔ Aim system functional
✔ Stealth/perception loop active
✔ Visibility is no longer binary

🚀 What comes next

👉 361–380 — Stealth System Depth + Ambush + Awareness

This is where:

hiding becomes tactical
ambush becomes real
perception becomes gameplay
🧠 Big Picture

You now have:

Combat ✔
Armor ✔
Weapons ✔
Distance ✔
Perception ✔

👉 This is a living system now, not just mechanics

If you want next:
I can design ambush, hiding, and awareness loops (this is where things get very DR-like).

I noticed one omission.  You mentioned a retreat command but we also need an advance command (I think) or is advance implied when you type attack? Maybe it should be automatic.  If someone retreats, you might have to type advance but otherwise  its automatic when you type attack <target>

You’re thinking about this exactly the right way—and you’re right to question it before locking behavior in.

Short answer:

Advance should be automatic on attack, but manual when reacting to retreat.

That matches both:

DR feel
good UX (low friction, high control)
🧠 The correct model (clean and scalable)
Default behavior
Case 1 — Normal combat start
attack goblin

System should:

set target
auto-advance to melee if not already there
perform attack

👉 No need for player to type advance here

Case 2 — Target has retreated
goblin retreats to missile range

Now:

attack goblin

Should:

fail with message
You are too far away to attack in melee.

Player must:

advance goblin

👉 This creates tactical intent

🎯 Why this is the right design
1. Reduces friction (important early)

Players don’t have to:

advance → attack → advance → attack
2. Preserves tactical depth

Retreat becomes meaningful because:

it forces a decision
it costs time/actions
3. Matches player expectation

Players expect:

“attack = go hit thing”

Not:

“attack = error unless I prepped range”

⚖️ Final Rule Set (lock this in)
Rule 1 — Attack auto-engages

If:

no range set OR
not explicitly separated

Then:

set_range(target, "melee")
Rule 2 — Retreat breaks engagement

If:

target at "missile"

Then:

melee attack fails
requires advance
Rule 3 — Ranged ignores melee restriction
ranged weapons can fire at distance
penalty if too close
🧱 Minimal implementation tweak (important)
Update MT346 logic

Instead of hard fail only:

current_range = self.caller.get_range(target)

if not weapon.db.is_ranged:
    if current_range != "melee":
        # AUTO-ADVANCE ONLY IF NOT EXPLICITLY SEPARATED
        if current_range == "unknown" or current_range is None:
            self.caller.set_range(target, "melee")
        else:
            self.caller.msg("You are too far away to attack in melee.")
            return
🧠 Optional (future polish, not now)

You could later support:

attack goblin advance

or:

auto_advance = True

But don’t add this yet—keep it simple.

🎯 Final Answer
Yes, you need an advance command
No, players should NOT need to use it every time
Attack should auto-advance unless the target has explicitly retreated
👍 Design locked

Attack = engage

Retreat = break engagement

Advance = re-engage

If you want next, I can tighten:

roundtime cost for advance/retreat
interrupt rules (very DR-like and important)
And mobile mobs should try to advance on you as you retreat.  Players should have a bit of advantage to escape combat but the npc should try to follow if they are winning the fight. Same is true in reverse if an npc is badly hurt it might try to retreat but the player should have an advantage 

Good—this is exactly the kind of asymmetry that makes combat feel alive instead of turn-based.

You’re describing three things that DR does very well:

Pursuit vs disengage is contested
Behavior depends on state (winning/losing)
Players have slight advantage over NPCs

Let’s lock this into a clean system.

🧠 Core Model (lock this in)
1. Range is not just a value — it’s pressure

When someone retreats:

distance increases
engagement is not instantly broken
opponent can respond
2. Retreat is an attempt, not a guarantee
You attempt to retreat...

Success depends on:

your agility / reflex
opponent pressure
current combat state
3. NPCs react based on combat state
Winning → aggressive pursuit
Losing → attempt escape
Neutral → probabilistic
🎯 Behavior Rules
Player retreats
Outcome options:
Clean escape
Partial retreat (still pressured)
Failed retreat (held in melee)
NPC reaction
If winning:
attempts to advance immediately
If losing:
may retreat instead of pursuing
Player advantage

Players get:

slight bonus to retreat success
slightly better disengage odds
🧱 Implementation Tasks (add these after 341–360)
🧱 MT361 — Add retreat resolution (not automatic success)

File: cmd_retreat.py

Replace instant set_range with:

retreat_roll = random.randint(1, 100)

player_score = self.caller.db.stats.get("agility", 10) + retreat_roll
enemy_score = target.db.stats.get("reflex", 10) + random.randint(1, 100)

# player advantage
player_score += 10

if player_score > enemy_score:
    self.caller.set_range(target, "missile")
    self.caller.msg("You successfully retreat!")
else:
    self.caller.msg("You fail to disengage!")
🧱 MT362 — Add partial retreat state
if player_score > enemy_score - 10:
    self.caller.set_range(target, "reach")
    self.caller.msg("You manage to pull back slightly.")
🧱 MT363 — Add NPC combat evaluation

File: typeclasses/npc.py

def is_winning(self, target):
    return self.db.hp > target.db.hp
🧱 MT364 — Add NPC pursue behavior
def attempt_pursue(self, target):
    if self.is_winning(target):
        self.set_range(target, "melee")
        self.location.msg_contents(f"{self.key} presses the attack!")
🧱 MT365 — Hook pursue into combat tick

In NPC combat loop:

if self.get_range(target) != "melee":
    self.attempt_pursue(target)
🧱 MT366 — Add NPC retreat behavior
def attempt_retreat(self, target):
    if self.db.hp < (self.db.max_hp * 0.3):
        self.set_range(target, "missile")
        self.location.msg_contents(f"{self.key} attempts to flee!")
🧱 MT367 — Add retreat chance for NPC
if random.randint(1, 100) < 30:
    self.attempt_retreat(target)
🧱 MT368 — Add player pursue advantage

When player advances:

self.caller.set_range(target, "melee")
# no roll needed

👉 Players re-engage faster than NPCs

🧱 MT369 — Add pursuit delay for NPCs

NPC advance should not be instant:

if random.randint(1, 100) < 70:
    self.set_range(target, "melee")
🧱 MT370 — Add messaging for pursuit
"You close the distance on {target}."
"{npc} rushes forward to keep you engaged!"
🧱 MT371 — Add disengage success state
self.db.in_combat = False
target.db.in_combat = False
🧱 MT372 — Add combat persistence check

If:

both are missile range for X ticks

Then:

break combat
🧱 MT373 — Add pressure mechanic (lightweight)
def get_pressure(self, target):
    return self.get_skill("combat") + self.db.stance["offense"]

Used later for:

retreat difficulty
pursuit strength
🧱 MT374 — Integrate pressure into retreat
enemy_score += target.get_pressure(self.caller)
🧱 MT375 — Add fatigue impact
player_score -= self.caller.db.fatigue * 0.2
🧱 MT376 — Add balance impact
player_score += self.caller.db.balance * 0.1
🧱 MT377 — Add roundtime to retreat
self.caller.set_roundtime(1.5)
🧱 MT378 — Add roundtime to NPC pursuit

NPC pursuit not instant—respect tick timing

🧱 MT379 — Validate player escape loop

Test:

retreat → advance → retreat → pursue

Expect:

dynamic back-and-forth
🧱 MT380 — Full system validation

Confirm:

retreat is not guaranteed
NPCs pursue when winning
NPCs retreat when losing
players have slight advantage
combat flow feels dynamic
🎯 Result

After this:

✔ Combat has momentum
✔ Distance is contested
✔ NPCs feel alive
✔ Escape is possible but not free
✔ Players feel slightly favored