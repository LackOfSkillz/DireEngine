MT-513c used Option 1 only: [c:/Users/gary/dragonsire/web/static/webclient/css/direbuilder.css](c:/Users/gary/dragonsire/web/static/webclient/css/direbuilder.css) now gives `.direbuilder-zone-score-panel` `z-index: 45`, which is enough to clear the competing sticky layer at `z-index: 5` on `.direbuilder-sticky-header`, with no ancestor stacking-context trap found on the score header chain. To ensure the fix was actually served, [c:/Users/gary/dragonsire/web/templates/webclient/direbuilder.html](c:/Users/gary/dragonsire/web/templates/webclient/direbuilder.html) advanced the DireBuilder stylesheet asset version to `?v=21`; live validation on `/direbuilder/` confirmed the breakdown panel opens fully above the Room Editor content with no obstruction, outside-click still closes it, Escape still closes it, the tooltip popover remains functional above the panel at `z-index: 60`, and three reopen cycles produced identical panel bounds with no layout drift. Screenshot captured during validation showed the panel open and unobstructed.
