- DRG-NEW-LANDING-CROSSING-REBUILD-005 extended `world/areas/the_crossing/import_canonical.py` with `PHASE5_ROOM_IDS`, `PHASE5_ROOM_SPECS`, `phase5_entries_from_map(...)`, and `ensure_canonical_crossing_phase5(...)`, then wired Phase 5 startup bootstrap and export surface without modifying the Phase 1, Phase 2, Phase 3, or Phase 4 importer functions.
- Final shipped Phase 5 import count is `35` rooms: five large districts (`Esplanade Eluned`, `Riverpine Circle`, `Riverlace Lane`, `Immortals' Walk`, `Mongers' Bazaar`) plus the promoted bridge rooms `739`, `741`, and `947`.
- Validation anchors for closeout: focused Phase 5 `5 passed`; clean changed-surface cluster `34 passed, 50 subtests passed`; preservation `315 passed, 153 subtests passed`; Ranger-adjacent `92 passed, 138 subtests passed`.
- Step 0 corrections that matter for later planning: canonical Crossing contains no `[[The Crossing, Hallway]]` rooms; Temple's `8` external exits all leave the canonical Crossing room set; the direct Phase 1-4 overlap into Phase 5 was only `5` bridge destinations (`739`, `741`, `826`, `899`, `16508`).
- Live steady-state smoke proved idempotent Phase 5 counts `1 -> 35 -> 35` and total Phase 1-5 canonical count `201`, with landing still at `788 / Town Green North`.
- Live reachability proof is mixed and must be recorded honestly: `Esplanade Eluned` and `Immortals' Walk` are arrival-reachable in the current directed graph, while `Riverpine Circle`, `Riverlace Lane`, and `Mongers' Bazaar` are not. Riverpine remains behind the unreachable `745 -> 824 -> 825` corridor, Riverlace remains behind the unreachable `16505 -> 16506 -> 16507` corridor, and Mongers is outbound-connected to `946` / `954` / `7905` without any imported inbound edge back into `947`.
- Live queue facts after Phase 5: `108` pending exits remain across Phases 1 + 2 + 3 + 4 and `122` combined pending exits remain across Phases 1 + 2 + 3 + 4 + 5 (`111` distinct destinations) for later small-district and corridor work.
- Hard ceiling preserved: `world/areas/the_crossing/import_canonical.py` ended at `830` lines.
- Honest caveats: the broader requested cluster still trips an older environment-sensitive Phase 4 assertion around room `813`, earlier broader clusters remain vulnerable to intermittent SQLite lock failures, and this SQLite runtime still does not expose the object-linked orphan columns used by the earlier orphan-count harness.