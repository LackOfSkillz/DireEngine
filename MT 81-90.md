Microtask 81 — Add NPC Flag to Characters
File:
typeclasses/characters.py
Task:

Add an NPC flag so we can distinguish players vs NPCs.

Implementation:

Inside at_object_creation():

self.db.is_npc = False
Result:
All characters default to player-controlled
NPCs will explicitly set this to True later
Validation:
Reload Evennia
Create character
Verify char.db.is_npc == False
Result:
MT81 PASS
Microtask 82 — Create Basic NPC Type
File:
typeclasses/npcs.py (NEW FILE)
Task:

Create a basic NPC class.

Implementation:
from typeclasses.characters import Character

class NPC(Character):
    def at_object_creation(self):
        super().at_object_creation()
        self.db.is_npc = True
Result:
NPCs inherit full combat + injury + RT system
Validation:
Reload Evennia
Spawn NPC via shell
Confirm is_npc == True
Result:
MT82 PASS
Microtask 83 — Spawn Test NPC Command
File:
commands/cmd_spawnnpc.py (NEW)
register in default_cmdsets.py
Task:

Create a simple spawn command.

Implementation:
from evennia import create_object
from typeclasses.npcs import NPC
from evennia.commands.command import Command

class CmdSpawnNPC(Command):
    key = "spawnnpc"

    def func(self):
        npc = create_object(NPC, key="corl", location=self.caller.location)
        self.caller.msg("NPC spawned.")
Register:
self.add(CmdSpawnNPC())
Validation:
spawnnpc
look
See "corl"
Result:
MT83 PASS
Microtask 84 — NPC Auto-Acquires Target
File:
commands/cmd_attack.py
Task:

When a player attacks an NPC, the NPC targets back.

Add after engagement:
if target.db.is_npc:
    target.set_target(self.caller)
Result:
NPC enters combat immediately
Validation:
attack corl
corl stats → Target = player
Result:
MT84 PASS
Microtask 85 — NPC Basic Attack Loop Hook
File:
typeclasses/npcs.py
Task:

Add a simple AI hook method.

Implementation:
def npc_combat_tick(self):
    if not self.is_in_combat():
        return

    target = self.db.target
    if not target:
        return

    if target.location != self.location:
        self.set_target(None)
        return

    self.execute_cmd(f"attack {target.key}")
Result:
NPC knows how to attack, but not yet ticking
Validation:
No runtime errors
Result:
MT85 PASS
Microtask 86 — Add NPC Combat Ticker
File:
server/conf/at_server_startstop.py
Task:

Hook NPC AI into ticker

Implementation:

Inside existing ticker loop:

if character.db.is_npc:
    character.npc_combat_tick()
Result:
NPCs now act automatically
Validation:
Reload
No errors
Result:
MT86 PASS
Microtask 87 — Respect Roundtime in NPC AI
File:
typeclasses/npcs.py
Update:
if self.is_in_roundtime():
    return

Add at top of npc_combat_tick

Result:
NPC obeys same pacing rules as players
Validation:
NPC does not spam attack
Result:
MT87 PASS
Microtask 88 — Prevent NPC Attacking Dead Targets
File:
typeclasses/npcs.py
Add:
if not target.is_alive():
    self.set_target(None)
    return
Result:
NPC stops when player is defeated
Validation:
Kill player → NPC stops
Result:
MT88 PASS
Microtask 89 — NPC Combat Messaging Check
Task:

No code change

Verify:
Player attacks NPC → sees:
"You hit corl..."
NPC attacks player → sees:
"corl hits you..."
Result:
Messaging flows both ways correctly
Result:
MT89 PASS
Microtask 90 — Full Combat Loop Validation
Scenario:
spawnnpc
attack corl
observe:
corl targets player
corl attacks back (with RT pacing)
disengage
corl stops attacking
Expected:
bidirectional combat works
RT respected
disengage breaks loop
no spam or crashes
Result:
MT90 PASS
✅ What You Just Unlocked

This is a major milestone:

You now have:

real combat loop
pacing (RT)
mutual engagement
disengage
injuries + bleed
NPC retaliation

👉 This is the first true “DragonRealms-feeling loop”